# DrawIO 绘图标准

Meta-model-agent 产出 DrawIO 技术路线图/过程图/架构图时参考此文件。

## 工具链

- `draw.io.exe`（已打包在 `runtime/draw.io/`，PATH 已注入）

- 导出：`draw.io.exe --export --format pdf --crop --output 图表/xxx.pdf 图表/xxx.drawio`

- ⛔ 务必加 `--crop` 裁剪白边

## ⛔ 零容忍准则（CLI headless 导出踩坑总结）

1. **禁止 `shadow=1`**：CLI 导出时 shadow 导致节点不渲染

2. **禁止 XML 注释 `<!-- -->`**：浪费 token，可能导致解析错误

3. **矩形一律直角**：普通矩形和容器统一使用 `rounded=0`，不得设置圆角；菱形、圆形、六边形、圆柱、平行四边形等已有语义形状保持不变。`shape=callout` 等 CLI 不稳定形状仍禁止使用

4. **换行用 HTML**：`html=1` 模式下用 `&lt;br&gt;` 换行，全部节点务必有 `html=1`。⛔ 涵盖 edge 上的标签文字节点（如判定分支的"是/否"标签）——若标签的 value 里涵盖 `&lt;font&gt;` 等 HTML 标签但 style 里没有 `html=1`，导出后会呈现原始 HTML 代码而不是渲染后的文字

5. **ID 全局唯一**，不可重复

6. **XML 转义**：`<>&"` → `&lt;&gt;&amp;&quot;`

7. **各个 edge 务必有子元素**：`<mxGeometry relative="1" as="geometry"/>` 不可自闭合

8. **中文直接写 UTF-8**

9. **页面背景透明**：`mxGraphModel` 加 `background=none`，并设 `page="0"` 去掉页面边框，避免导出 PDF 有灰色背景

10. **架构图/变量关系图用 TikZ 而非 DrawIO**：CLI 导出不支撑 ELK 自动化路由，`orthogonalEdgeStyle` 的连线会穿过中间节点。需精确连线路由、数学公式、几何图形的图继续用 TikZ

11. **图内不写标题**：标题由 LaTeX `\caption{}` 统一管理，DrawIO 图内不要放标题文字节点（避免标题重复、字体不一致）

## DrawIO vs TikZ vs AI Image 完整分工表

全部 20 种非数据图的推荐工具一览。规划阶段直接查此表。

| # | 图类别 | DrawIO | TikZ | AI Image | 选取理由 |

|---|--------|--------|------|-----------|---------|

| 1 | 技术路线图 | ✅ 首选 | | | 分阶段分组框+粗箭头，连线简单 |

| 2 | 论文结构图 | ✅ 首选 | | | 章节层次+箭头，类似技术路线图 |

| 3 | 子问题求解过程图 | ✅ 首选 | | | 纵向环节+判定分支，连线只走上下 |

| 4 | 算法过程图（带公式） | | ✅ 首选 | | 需 LaTeX 公式渲染（如 $\Delta f < \epsilon$） |

| 5 | 算法过程图（无公式） | ✅ 可用 | ✅ 可用 | | 无公式时 DrawIO 更快；有判定分支多时 TikZ 更精确 |

| 6 | 数据处理 Pipeline | ✅ 首选 | | | 横向多阶段卡片，连线简单 |

| 7 | 模型架构图（神经网络/集成学习） | | ✅ 首选 | | 需跨层精确连线，DrawIO CLI 连线会穿过节点 |

| 8 | 概念框架图（简单分层） | ✅ 首选 | | | 嵌套色块+层间大箭头，无跨层连线 |

| 9 | 概念框架图（复杂连线） | | ✅ 首选 | | 有跨层箭头+标注系数，需精确路由 |

| 10 | 变量关系/因果路线图 | | ✅ 首选 | | 需精确控制箭头路线+标注系数/显著性 |

| 11 | 指标体系层次图 | ✅ 首选 | | | 目标层→准则层→指标层的树形结构，连线只走纵向 |

| 12 | 模型选取决策树 | ✅ 首选 | | | 树形分支结构，连线简单 |

| 13 | 场景示意图（物理/工程） | | | ✅ 首选 | 需写实渲染（无人机/传感器/交通等），代码画不出来 |

| 14 | 几何示意图 | | ✅ 首选 | | 需精确坐标、虚线、角度标注、数学变量 |

| 15 | 网络拓扑图（≤15节点） | ✅ 可用 | ✅ 可用 | | 节点少时 DrawIO 够用；需精确布局时用 TikZ |

| 16 | 网络拓扑图（>15节点） | | ✅ 首选 | | 节点多时需算法布局（spring/kamada_kawai），或用 matplotlib networkx |

| 17 | 网络路线图（标注最优路线） | | ✅ 首选 | | 需在图上精确标注路线+权重，DrawIO 人工排太麻烦 |

| 18 | 甘特图/调度方案图 | ✅ 可用 | | | 调度类赛题的结果展示；竞赛工作安排不需画图。简单甘特图用 DrawIO，复杂调度用 matplotlib |

| 19 | 灵敏度分析示意图（定性） | ✅ 可用 | ✅ 可用 | | 简单箭头标注用 DrawIO；带公式用 TikZ |

| 20 | 方法对比矩阵图 | ✅ 首选 | | | 表格/矩阵表现形式，DrawIO 画格子方便 |

**速查准则：**

- 需 LaTeX 公式 → TikZ

- 需跨区精确连线（箭头不可穿过节点） → TikZ

- 需写实场景渲染 → AI Image

- 其余（分层/分组/树形/过程/卡片） → DrawIO

## ⛔ 通用设计标准（全部 DrawIO 图务必遵守）

以下标准适用于全部类别的 DrawIO 图（技术路线图/过程图/框架图/指标体系图等）。

### 容器与层次

- **用 swimlane 容器表示阶段/分组**：各个逻辑阶段用 `swimlane` 容器包裹，有标题栏+淡色背景。子节点用 `parent="容器id"` + 相对坐标

- **不同工作区用灰阶层次区分**：容器背景在白色、浅灰和极浅灰之间交替；主要层级依靠边框粗细、虚实和留白表达，不依赖多彩填充

- **容器标题栏**：`startSize=22`，字号 12pt，加粗，写明"第N阶段：阶段名称"

### 节点设计

- **⛔ 全部矩形节点强制直角**：普通节点、核心节点、输出节点、分组容器和表头均使用 `rounded=0`；检查器发现矩形 `rounded=0` 时直接失败
- 禁止阴影和装饰渐变；默认白底、灰阶边框，仅允许一个低饱和强调色标识关键节点

- **节点内容要丰富**：主标题（加粗 9-10px）+ 灰色副标题（8px，`color:#666`），用 `<br>` 换行。避免只写一个词

- **关键节点突出**：用加粗黑灰边框（`strokeWidth=2.2`）+ 极浅灰填充（如 `fillColor=#F2F2F2`）+ ★ 前缀标记

- **结论/结果节点**：用浅灰填充、双线或加粗边框配合 ✓ 标记；仅在确有风险语义时使用深灰虚线，不以红绿作为主要区分

- **负面/异常节点**：用深灰虚线边框（`strokeColor=#555555;dashed=1`）标注，如"虹吸效应""普通面板回归"

- **普通节点**：白底 + 中灰边框（`fillColor=#FFFFFF;strokeColor=#666666`）

### 连线标准

- **用 source/target 属性连线**：`edge="1" source="n1" target="n2"`，避免用人工坐标点。DrawIO 会自动化计算连接位置

- **同容器内连线**：`parent="容器id"`，统一使用深灰或黑色细线

- **跨容器过渡箭头**：`parent="1"`，用粗灰色（`strokeWidth=4;strokeColor=#C0C0C0;endArrow=block;endFill=1;endSize=8`）

- **决策分支**：是/否通过实线与虚线、标签或箭头样式区分，标签紧贴箭头，避免依赖颜色

### ⛔ 连线路由准则（7 条，防止连线穿过节点）

1. **禁止多条边共享同一路线**：若两条边连接同一对节点，务必从不同位置出入。用 `exitY=0.3` 和 `exitY=0.7` 区分，避免都走 0.5

2. **双向连接用对侧**：A→B 从 A 的右侧出（`exitX=1`），进 B 的左侧（`entryX=0`）；B→A 反过来

3. **务必明示指定 exitX/exitY/entryX/entryY**：每条边的 style 里都要写这 4 个属性，避免让 DrawIO 自动化猜

4. **连线务必绕开中间节点**：画边此前先看源和目标之间有没有其他节点挡着，有的话用 waypoints 绕行，留 20-30px 间距

5. **先规划布局再画线**：把节点按流向分层/分列排好，留出连线通道（150-200px 间距），再画边

6. **复杂路由用多个 waypoints**：一个拐点不够就用 2-3 个，形成 L 形或 U 形路线，各个方向变化都需一个 waypoint

7. **连接点选自然方向**：上下流向从底部出（`exitY=1`）顶部进（`entryY=0`）；左右流向从右侧出（`exitX=1`）左侧进（`entryX=0`）。避免用角落连接点（如 `exitX=1,exitY=1`）

**画完后自检**：有没有边穿过非源/目标的节点？有没有两条边重叠？有没有用了角落连接点？

### 整体布局

- **画布宽度 920-950px**：内容区充足宽，节点不挤

- **容器间距 15-17px**：过渡箭头占据这个间距

- **节点宽度 130-220px**：依据文字长度调整，中文节点 ≥ 字数×14+40px

- **节点高度 28-48px**：单行 28px，双行（主标题+副标题）40-48px

- **同行节点等距分布**：计算容器宽度，均匀分配

- **⛔ 节点务必居中分布**：若一行只有 3 个节点但容器宽度能放 4 个，节点要居中排列，避免左对齐留大片空白。计算方法：`左边距 = (容器宽度 - 节点总宽度 - 节点间距总和) / 2`。最后一行（结论/交付）尤其容易出现左对齐问题，务必核验

### 配色方案（swimlane 容器）——黑白学术风格

⛔ 默认使用黑、白与灰阶，确保打印、复印和灰度显示时仍能辨认。可保留一个低饱和强调色，但只能用于极少数关键节点，不能按工作节点逐色装饰。

| 阶段序号 | 容器填充 | 容器边框 | 标签填充 | 节点边框 | 适合内容 |

|---------|---------|---------|---------|---------|---------|

| 1 | `#FFFFFF` | `#555555` | `#333333` | `#555555` | 背景/问题/输入 |

| 2 | `#F7F7F7` | `#777777` | `#444444` | `#666666` | 数据/指标/构建 |

| 3 | `#EEEEEE` | `#666666` | `#333333` | `#555555` | 分析/检验/探索 |

| 4 | `#FFFFFF` | `#444444` | `#222222` | `#444444` | 关键模型/建模 |

| 5 | `#F5F5F5` | `#777777` | `#444444` | `#666666` | 稳健性/检验 |

| 6 | `#EAEAEA` | `#555555` | `#222222` | `#444444` | 结论/推荐 |

节点配色原则：

- 普通节点：白底（`#FFFFFF`）+ 中灰边框，边框宽度 1.3-1.5px

- 关键/高亮节点：极浅灰填充（`#F2F2F2`）+ 深灰边框 2.2px

- 结论/通过节点：浅灰填充（`#E8E8E8`）+ 黑灰双线或加粗边框

- 负面/失败节点：白底 + 深灰虚线边框

- 连线颜色：统一使用 `#555555` 或 `#333333`，宽度 1.3-1.5px

- 过渡箭头：中灰（`#B8B8B8`），宽度 4px

⛔ 节点数量控制：各个阶段/容器内最多 2 行节点，每行最多 4 个。总节点数不超过 30 个。宁可精简也避免塞太多——杂乱比简洁更差。

### swimlane 容器样式模板

```xml

<mxCell id="DISCOVERY" value="&lt;b&gt;第一阶段：阶段名称&lt;/b&gt;"

  style="swimlane;startSize=22;fillColor=#F7F7F7;strokeColor=#666666;strokeWidth=1.5;rounded=0;html=1;fontSize=12;fontStyle=1;swimlaneLine=0;arcSize=4;"

  vertex="1" parent="1">

  <mxGeometry x="30" y="10" width="920" height="80" as="geometry"/>

</mxCell>

```

### 节点样式模板

```xml

<!-- 普通节点（白底+阶段色边框+双行文字） -->

<mxCell id="n1" value="&lt;b&gt;主标题&lt;/b&gt;&lt;br&gt;&lt;font style=&quot;font-size:8px;color:#666;&quot;&gt;灰色副标题说明&lt;/font&gt;"

  style="rounded=0;whiteSpace=wrap;html=1;fillColor=#FFFFFF;strokeColor=#666666;strokeWidth=1.5;fontSize=12;fontStyle=1;"

  vertex="1" parent="DISCOVERY">

  <mxGeometry x="15" y="28" width="175" height="42" as="geometry"/>

</mxCell>

<!-- 核心节点（加粗边框+浅色填充+★标记） -->

<mxCell id="n_core" value="&lt;b&gt;★ 核心模型名称&lt;/b&gt;&lt;br&gt;&lt;font style=&quot;font-size:8px;color:#666;&quot;&gt;关键参数说明&lt;/font&gt;"

  style="rounded=0;whiteSpace=wrap;html=1;fillColor=#F2F2F2;strokeColor=#333333;strokeWidth=2.2;fontSize=12;fontStyle=1;"

  vertex="1" parent="EVIDENCE">

  <mxGeometry x="225" y="28" width="200" height="48" as="geometry"/>

</mxCell>

<!-- 结论节点（绿色填充+✓标记） -->

<mxCell id="n_ok" value="&lt;b&gt;✓ 结论成立&lt;/b&gt;"

  style="rounded=0;whiteSpace=wrap;html=1;fillColor=#E8E8E8;strokeColor=#333333;strokeWidth=2;fontSize=12;fontStyle=1;"

  vertex="1" parent="COMPUTATION">

  <mxGeometry x="660" y="28" width="155" height="48" as="geometry"/>

</mxCell>

```

### 连线样式模板

```xml

<!-- 同容器内连线（用 source/target） -->

<mxCell id="e1" edge="1" source="n1" target="n2" parent="DISCOVERY"

  style="rounded=0;html=1;strokeWidth=1.5;strokeColor=#555555;endArrow=classic;endFill=1;">

  <mxGeometry relative="1" as="geometry"/>

</mxCell>

<!-- 跨容器过渡箭头（粗灰色） -->

<mxCell id="arr12" edge="1" parent="1"

  style="rounded=0;html=1;strokeWidth=4;strokeColor=#C0C0C0;endArrow=block;endFill=1;endSize=8;">

  <mxGeometry relative="1" as="geometry">

    <mxPoint x="490" y="88" as="sourcePoint"/>

    <mxPoint x="490" y="105" as="targetPoint"/>

  </mxGeometry>

</mxCell>

```

## 布局模板参考（不强制，可自由发挥）

以下模板只提供拓扑和间距参考。采用模板时必须统一改为直角矩形、纯色或灰阶、无阴影；不得复制其旧有粉色表头、渐变和卡片化装饰。

**⛔ 模板选取准则：按问题逻辑和节点数量选择最合适的拓扑，不得随机选模板，不得为了风格变化牺牲信息一致性。**

| 模板 | 文件 | 风格特点 | 配色 |

|------|------|---------|------|

| B  | `example_roadmap_stats.drawio` | 简版四阶段三栏拓扑 | 灰阶 + 一个低饱和强调色 |

| B-warm | `example_roadmap_stats_warm.drawio` | 简版四阶段替代拓扑 | 灰阶 + 一个低饱和强调色 |

| C | `example_roadmap_hex.drawio` | 完整六阶段三栏拓扑 | 灰阶 + 一个低饱和强调色 |

| C-cool | `example_roadmap_hex_cool.drawio` | 完整六阶段替代拓扑 | 灰阶 + 一个低饱和强调色 |

选取方法：

```bash

# 按节点数量和依赖拓扑选择模板（禁止随机）

TEMPLATE=$(python3 -c "import random; print(random.choice(['B', 'B-warm', 'C', 'C-cool']))")

echo "本次使用模板: $TEMPLATE"

case "$TEMPLATE" in

  B)      cat 工具/example_roadmap_stats.drawio ;;

  B-warm) cat 工具/example_roadmap_stats_warm.drawio ;;

  C)      cat 工具/example_roadmap_hex.drawio ;;

  C-cool) cat 工具/example_roadmap_hex_cool.drawio ;;

esac

```

**配色避免混搭**：选定模板后，整张图沿用该模板的配色。避免把 B-warm 的橙色和 C-cool 的蓝色混在一张图里。

### 模板 B：简版四阶段（example_roadmap_stats）

三栏结构：直角灰阶表头 + 语义阶段形状 + 直角内容模块 + 克制分组。适合阶段较少（3-4 个）、内容紧凑的研究。

**布局结构**：

- 顶部：三个表头（研究阶段 | 研究内容 | 研究方法），粗箭头向下引导

- 左列：六边形阶段节点（提出问题 → 分析问题 → 建模预测 → 政策推荐），粗灰色箭头连接

- 中列：虚线框包裹每阶段的内容模块

- 右列：方法节点列出每阶段采用的具体方法

- 横向连接：阶段→内容（空心箭头），内容→方法（虚线箭头）

**配色（B）**：白底、#F2F2F2 浅灰填充、#666666 边框；只对一个关键阶段使用低饱和强调色

**配色（B-warm）**：沿用同一灰阶合同，仅拓扑和间距与 B 不同

### 模板 C：完整六阶段（example_roadmap_hex）

模板 B 的扩展版，6 个阶段（提出问题→分析问题→模型构建→实验校验→结果分析→结论与展望），每阶段 6-7 个子节点。适合内容丰富、需展示完整研究链条的论文。

**布局结构**：

- 顶部：三个表头（研究阶段 | 研究内容 | 研究方法）

- 左列：6 个六边形阶段节点纵向排布

- 中列：每阶段一个虚线框，内含 4-7 个内容子节点（含横向小箭头连接）

- 右列：每阶段相应一个方法节点（多行阐明该阶段的方法/工具）

**配色（C）**：白底、#F2F2F2 浅灰填充、#666666 边框；只对一个关键阶段使用低饱和强调色

**配色（C-cool）**：沿用同一灰阶合同，仅拓扑和间距与 C 不同

**适用场景**：全部论文类别，尤其适合内容丰富、阶段层次清晰的研究。

⛔ **完整 XML 示例**：工具/example_roadmap_stats.drawio、工具/example_roadmap_stats_warm.drawio、工具/example_roadmap_hex.drawio、工具/example_roadmap_hex_cool.drawio。仅按节点数量和拓扑选取结构参考，不得随机选择；采用后必须保持直角矩形、无渐变、无阴影，并替换为真实论文内容。

## 实践核验清单

产出每张 .drawio 后务必逐条自检：

- [ ] **字号核验**：最小字号 ≥ 10pt，普通节点 ≥ 12pt，标题 ≥ 13pt

- [ ] **重叠核验**：相邻节点边界框不交叉，间距 ≥ 8px

- [ ] **对齐核验**：同行节点 y 坐标保持一致，同列节点 x 坐标保持一致

- [ ] **连线遮挡核验**：无连线穿过节点或文字

- [ ] **文字溢出核验**：节点完全涵盖文字，中文节点 width ≥ 字数×14+30px，英文节点 width ≥ 字符数×8+30px。⛔ 若节点内有换行（`<br>`），height 需额外加 16px/行

- [ ] **样式一致性**：同类节点 fillColor/strokeColor/fontSize 统一

- [ ] **容器核验**：子节点 parent 指向无误的容器 id，坐标是相对坐标

- [ ] **edge 核验**：各个 edge 有 `<mxGeometry relative="1" as="geometry"/>`

- [ ] **无 shadow**：确认没有 `shadow=1`

- [ ] **无 XML 注释**：确认没有 `<!-- -->`

- [ ] **html=1 核验**：全部 mxCell（涵盖 edge 标签节点）的 style 里都有 `html=1`。若 value 涵盖 `&lt;font&gt;`/`&lt;b&gt;`/`&lt;br&gt;` 但没有 `html=1`，导出后会呈现原始 HTML 代码

- [ ] **紧凑度**：阶段间过渡箭头间距 ≤ 15px，整图避免拉得太长

- [ ] **填充度**：子分组框/节点务必填满层宽度，不留大片空白。两个分组框之间间距 15px

- [ ] **居中核验**：每行节点数少于容器能容纳的最大数量时，节点务必居中分布，避免左对齐。尤其核验最后一行（结论/交付阶段）

- [ ] **尺寸控制**：DrawIO 图不需限制高度，LaTeX 插入时用 `keepaspectratio` + `height` 双约束自动化处理。专注于内容完整、节点不遮挡即可

## 技术路线图 vs 求解过程图（⛔ 务必区分）

| | 技术路线图 | 求解过程图 |

|---|---|---|

| 视角 | 全局——整篇论文从问题到结论 | 局部——单个子问题从输入到交付 |

| 内容 | 多个阶段，每阶段多个节点，展示整体求解思路 | 一个问题的具体环节、判定分支、数据流向 |

| 放置 | 问题重述章节末尾 | 各子问题章节开头 |

| 数量 | 整篇论文 1 张 | 各个子问题 1 张 |

**布局不限制**——Meta-model-agent 可依据具体赛题内容自由发挥布局和视觉设计（纵向/横向/混合/分区/嵌套都可），只要遵守：

1. 零容忍准则（无 shadow、无 XML 注释、无特殊形状等）

2. 通用设计标准（swimlane 容器、source/target 连线、双行节点、阶段配色）

3. 连线路由准则（不穿过节点、明示指定 exitX/exitY）

4. 图内不写标题（由 LaTeX caption 管理）

⛔ 唯一的硬限制：技术路线图和求解过程图不可长得一样——技术路线图要体现"全局多阶段"的视觉层次，求解过程图要体现"单问题环节链"的逻辑流向。

### ⛔ 求解过程图最低要求（不可只画一条直线）

求解过程图务必体现问题求解的逻辑复杂性，不可只是一串节点从上到下排列。最低要求：

1. **不少于 1 个判定分支**（菱形节点 + 是/否标签）——如"数据是否满足正态性？"、"模型精度是否达标？"、"约束是否满足？"

2. **不少于 1 处并行/分叉**——如数据预处理和特征工程与此同时开展，或两种方法对比后选优

3. **节点双行**——主标题（加粗）+ 灰色副标题（具体方法/参数阐明）

4. **循环反馈**（若有）——参数不满足→调整→重新求解的回路箭头

5. **颜色区分环节类别**——输入（蓝色）、处理（绿色）、判定（黄色）、检验（紫色）、交付（红色）

参考示例布局：主过程纵向排列，遇到判定分支时左右分叉（是→继续主过程，否→右侧替代方案或调整参数后回路）。默认使用灰阶，通过形状、线型和位置区分环节；仅允许一个低饱和强调色标识关键步骤。

**⛔ 求解过程图绝对避免画右侧工具/方法注释栏**（"工具与方法"那种独立侧栏是技术路线图专属）。求解过程图应简洁，只展示求解环节和逻辑分支，**禁止**在右侧放 pandas/matplotlib/SciPy/numpy 等工具标签。

**⛔ 也避免把工具/库名（如 SciPy minimize、Python DEAP、scikit-learn 等）写进节点副标题。** 副标题只描述**环节本身做什么**（输入/交付/方法的物理或数学含义），不写技术实现。例子：

- ❌ 错：「优化圆弧半径比 / SciPy minimize」

- ❌ 错：「需求热力分析 / scikit-learn KDE」

- ✅ 对：「优化圆弧半径比 / 最短路线目标函数」

- ✅ 对：「需求热力分析 / 核密度估计识别热点」

⛔ **完整 XML 示例**：`工具/example_flow.drawio` 是一个可直接导出的完整求解过程图示例（有分叉、判定、循环反馈）。产出前先 `cat 工具/example_flow.drawio` 参考其 XML 结构和样式。

## 技术路线图产出过程

### 工作节点 1：查阅规划文档并确定内容

```bash

cat 问题分析.md 2>/dev/null || cat 选题规划.md 2>/dev/null || cat 论文规划.md 2>/dev/null

```

### 工作节点 2：确定工作结构

典型科研论文技术路线图 3-5 个阶段：

- 提出问题（背景/假设/数据）

- 分析问题（指标构建/探索性分析/模型选取）

- 解决问题（关键模型/效应分解/稳健性检验）

- 研究成果（结论/政策推荐）

### 工作节点 3：生成 .drawio XML

按三栏布局产出，用 swimlane 容器 + 相对坐标。

### 工作节点 4：导出 PDF

```bash

draw.io.exe --export --format pdf --crop --output 图表/技术路线图.pdf 图表/技术路线图.drawio

```

### 工作节点 5：校验 PDF 非空

```bash

ls -la 图表/技术路线图.pdf

```

若导出失败，核验：shadow=1？XML 注释？特殊形状？ID 重复？标签未闭合？
