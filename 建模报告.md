# 问题一建模与算法交接

> **2026-09-24 当前采用版本：r02 / routes_gate_reuse。** 本文保留研发历史，写作以 `审查/问题一交付与赛题要求核对_r02.md` 和 `协作/交接/Q1/Q1-WRITE-r02.md` 为当前入口。单图运行 `python 程序/q1_submit.py 数据/processed/q1/data/case_001.json -n 5 --output trial --verify-final`；批量运行 `python 程序/q1_reproduce.py --output all100 --workers 2`。输入尚未导入时先运行 `python 程序/主程序.py prepare`。当前程序包 `output/q1-delivery-r02-20260924-portable.zip`，校验后再运行。1～5核平均加速比：1、1.834917、2.546238、3.161009、3.665903。只报告我们自身工程；旧历史段落不是当前提交口径。


本报告描述已实现的研究版本，供问题一章节写作。正式工作流状态仍以 `状态/工作流状态.json` 为准，不据此宣称阶段完成。

模型定义 Q1 | academic_name: 考虑共享DDR与缓存约束的有向无环图划分—多处理器调度组合优化模型 | canonical_model_family: 有向无环图划分与带前驱约束的多处理器调度组合优化

模型结构 Q1 | solver_algorithm: 结构感知初始划分、关键路径调度与预算保护的有界启发式搜索

论文表达 Q1 | 展示名称: 共享DDR约束下的图划分与多核调度模型 | 问题角色: new_model | 继承模型: none | 核心方法: 完整分量与主导分量初解、局部成本排名及事件准入、预算保护的有界修复

## 1. 决策与目标

给定算子有向无环图 G=(V,E)，将非 COPY 操作集合 Vc 划分为互不相交且并集为 Vc 的子图 P={V1,...,Vq}；每个子图构成一个 Task。决策由划分 π、核心映射 m 和各核顺序 σ 组成。核心数 k∈{2,3,4,5}。商图中的依赖边与各核串行顺序边合并后必须无环；每个操作恰好属于一个子图，每个 Task 恰好分配一个核。

采用字典序目标 min (T(π,m,σ), B_add(π,m,σ))：首先最小化总完成时间，时间相同时选择额外搬运较少者。T 由赛题事件评估过程 E(G,π,m,σ;k,config) 给出，等于所有 Task 结束时刻的最大值。B_add 等于调度后 COPY 总字节数减去原图 COPY 字节数，包括划分额外搬运与核内缓存溢出搬运。这里是黑箱可计算的组合优化目标，不声称构建了可由 MILP 求解器精确求解的线性模型。

## 2. 时间、资源与通信约束

对于依赖 Task u→v，v 的释放时间不得早于 u 完成加跨核同步延迟；本配置跨核为 1000 cycles、同核数据前驱不另加该跨核等待。同一核的前一 Task 完成后还需要 100 cycles 的任务间隔。多个释放条件取最大值，不把所有前驱等待求和，也不把每条跨核边的 1000 直接加到目标函数。

同核连续执行不保留私有缓存，所以跨 Task 张量仍须经过 DDR。固定配置的 DDR 带宽为 60 bytes/cycle，L1 容量 524288 bytes，UB 容量 131072 bytes。容量不足通过官方核内调度插入的换入换出处理；各 Pipe 的操作顺序、数据依赖、内存复用依赖以及共享 DDR 的动态竞争共同决定事件开始与结束。四条 Pipe 可以在满足依赖时并行，不能把各自工作量相加当作 Task 时间。

最终评价使用附件官方语义。搜索代理则使用近似成本与已观察的局部核内时长，服务于候选筛选；代理误差尤其可能来自共享 DDR 和释放链变化。局部精确时长不等于全局精确 Makespan。任何执行完整全局仿真的候选检查都计入正式评价机会，不包装成免费代理。

## 3. 已采用算法及其理由

入口为 `程序/q1_submit.py`，实际搜索在 `程序/q1_experimental.py`。采用 shared_region，启用 local_cost、critical、insertion、comm_rank、partition_guard、shared_input、region 七项机制，预算 12、种子 0。代码中其他实验开关的存在不代表本轮结果使用了它们。尤其不能把 adaptive/portfolio/budget_adapt 或 region_gap 写成当前已经启用的特性。

1. 先评价整图单 Task 的保底方案，再生成拓扑贪心划分；优先利用输入复用与依赖关系，控制子图大小。其计算量低、易于解释，为后续提供合法初解。
2. 对合法商图作有界多层次无环粗化，再逐层展开和比较。共享输入信息用于识别仅看两两边权容易漏掉的重复读取代价。该模块借鉴多层次思想，不宣称完整复现某篇有向超图论文的全部算法。
3. 用关键路径优先级、通信相关排序与最早完成时间选择核心。基础映射支持合法空隙插入，同时满足两侧任务间隔；这是减少无谓排队的调度启发式，并非穷举所有顺序。
4. 保护基础粒度划分的搜索机会，再进行有限的划分、映射和顺序候选调整。关键区域修复选择至多 4 个关联 Task、至多 512 个操作；至多 6 组映射与 2 种优先级构造有限代理候选，再返回至多 2 个不同方案。区域外核心归属与相对顺序保持约束，所有候选验证合法性和无环性。本轮 region_gap 关闭，不能把一般映射的空隙插入误写成区域修复器也启用了该开关。
5. 最终接受由官方目标决定，保留字典序最优解。评价机会包含整图保底和命中缓存的候选，缓存不自动增加预算；候选产生尝试也有上限，不遍历所有划分、核心组合或任务排列。

选择这条路线，是因为合并减少重复搬运却会损失并行度，过度切分增加 DDR 与任务释放成本，单纯最小割或均分操作数都不能直接保证 Makespan 更好。多种初解探索不同粒度，局部联合修复改变少量相关决策，最终由统一评估处理非线性的带宽与缓存影响。在有限时间下，这比组合穷举可执行，也比只优化代理分数可靠；仍不保证全局最优。

研究初期讨论过 ALNS 等路线，仓库也保留实验实现。但当前全量采用方案不等于所有自适应机制全开。具体机制必须以冻结配置及源代码为准。

## 4. 全量实验与最优算例

采用固定单核基准定义 S_ik=T_i1/T_ik，平均值为 (1/100)∑S_ik。1～5 核平均加速比分别为 1、1.700389、2.264697、2.685511、2.990661。已交付 100 图×5 核共 500 条采用结果；对照另有 400 条。

以相对自身单核的加速比衡量，2～5 核的最高值均出现在 case_097：2.190063、3.274339、4.247084、5.273640；对应 Makespan 为 5247114、3509566、2705741、2179047 cycles，固定单核为 11491509 cycles。1 核全部算例加速比均为 1，无唯一第一。

case_097 的画像显示较高计算单元占用与较低关键链释放等待；单核基准还存在启发式导致的溢出，多核划分后的溢出为零，因此超过核数的加速比并不违反题意。但不能把所有收益因果归结为消除溢出：其单核额外搬运 10584064 bytes，多核额外搬运约 85295104～92076032 bytes，划分搬运反而更多。此例体现主要目标和次级目标的权衡。不同图原始工作量不同，绝对最短 Makespan 不是这里的“最优算例”定义。

## 5. 求解计算时间的优化与边界

`程序/q1_fast_evaluator.py` 在官方源码哈希锁定下，以剩余操作计数、活跃 Task 集合、张量反向索引和分 Task 时间线索引替代重复全表扫描，保持官方步骤、事件顺序及带宽运算。`程序/q1_fast_costs.py` 缓存固定成员顺序对应的基础成本，动态局部观测仍使用当前状态；缓存不能把已校准的旧状态带入新候选。

验证范围：500 个保存方案的完整评估输出逐项一致，500 个划分的基础成本冷/热缓存一致；原先耗时超过 600 秒的 6 个配置完整重跑搜索，候选序列、最终方案及官方结果一致。6 个配置的加速后搜索加固定单核计算为约 110～281 秒。另一次同机串行 case_084 单核评估从 18.354 秒到 2.917 秒，约 6.29 倍。这是特定评估调用的运行加速，不是任务执行加速；不能将不同机器、并发条件下的旧耗时与新耗时相除。尚未在新后端重跑全部 400 个搜索，不能宣称全部未来用例必定在 5 分钟内完成。

后续先测剩余耗时的组成，再优化重复图构建、局部拓扑处理与有完整输入/配置/评估器键的复用；固定单核结果每图仅算一次，采用内存约束下的进程并行。对冷/热缓存、串行/并行分别报告墙钟时间与峰值内存。若引入硬截止时间，需重新比较同预算质量与超时比例，不能悄悄减少候选后沿用当前质量结论。

## 6. 写作证据与限制

数值来源为 `图表/runs/20260923-A-q1-v18-merged/` 与 `...v18-analysis/`；写作五点与逐例表由 `程序/q1_writing_summary.py` 重建。结果一致性见 `...runtime-final-all100/`、`...runtime-cost-check/`、`...runtime-slowest-six/`、`...runtime-final-paired/`。算法符合性与复现交付分别见 `审查/问题一原基线赛题符合性审核_20260923.md`、`审查/问题一复现交付与大图运行加速_20260923.md`。

论文应报告局部代理误差、固定种子、启发式基准、有限预算与全量新后端搜索尚未重跑等限制；不声称最优性、90% 时间下降、全部新机制有效或正式提交审核已通过。文献引用须由写作者根据原文核实，不能由算法名称推断本实现就是某文献的完整算法。


## 7. 结构初始候选的独立验证

新增 `seed_components`、`seed_batches`、`seed_depth` 和 `seed_combined` 四个实验配置，不更改正式采用的 `shared_region`。目的是解决固定粒度初解可能遗漏的结构：多个相互独立分量可在核间并行，单个大分量则需要合法切分才能释放并行性。

首先去除输入输出 COPY 节点对计算任务归属的干扰，保留其传递的计算依赖，再求计算 DAG 的弱连通分量。完整分量分核用 LPT、按 Pipe 工作量均衡及轮转三种有界策略，把同核分量合成一个 Task；这些分量之间没有计算依赖，但仍可能重复读取同一 DDR 输入，且合并后可能增加缓存压力，不能当作免费的并行。分量分批保留每个分量完整性，以 256 个计算操作为目标批量，限制合并范围；该数量是粒度参数，不是内存容量约束。只有分量数量不少于两倍核数、且最大分量工作量不超过均摊工作量的 1.15 倍时才生成分批候选。

若最大分量超过该阈值，则生成宽度 8、16、4 的单调拓扑深度窗口。每个窗口内再次求弱连通分量，形成无环商图；按尾部关键路径优先级和估计完成时间选择核心及追加顺序。深度随有向边单调增长，组间依赖只向后，拓扑追加核心顺序不会引入调度环。估计搬运成本在同核跨 Task 时也计入 DDR，不假设跨 Task 私有缓存驻留。成本模型只是候选构造依据，最终接受仍由统一官方场景 A 评价决定。

新候选放在既有三个初解之后，规范化子图标签后去重，最多占四个评价机会，同时预留四档基础粒度尝试与后续搜索机会。每次求解仍为 12 个评价机会，包括固定单核结果复用一次。保护的是基础粒度的尝试额度，不保证后续候选与基线完全相同，因为新评价会改变成本观察与当前父解。应同时分析预算挤占和搜索路径改变。

对照采用原五图 017/045/048/065/077 加反例 002/028/063/067/085，2～5 核、种子 0，五方案共 200 次；另预先冻结种子 1/2 的五核基线及组合共 40 次。逐方案官方核验、配对结果和采用建议见 `审查/问题一结构初解同预算验证_20260923.md`。这十图是开发验证集，不是全量或盲测成绩。方法生成部分改编自用户提供工程，来源哈希见代码和实验协议，不能声称为原创算法。


## 8. 完整分量候选与结构预算控制（20260924 原型）

本轮继承问题一原模型，不新增目标或改变场景 A 的 DDR、同步与缓存语义。以完整计算弱分量为原子单位，只尝试一份静态筛选的布局，避免将外部工程全部候选追加进已有 12 次评价预算。

每个分量的权重取四条 Pipe 操作周期和的最大值。分量数量不足核数，或最大分量权重超过总权重/核数时，继续使用原算法；若每个分量的内部张量并集都超过某一私有存储容量，也保守地使用原算法。并集并不是活跃峰值，这不是合法性判断，只是避免在典型大工作集上盲目融合的路线规则。没有使用 case 编号或大图节点数量阈值。

布局候选最多为完整分量 LPT、Pipe 平衡与轮转三种，去重后按每核必要 Pipe 工作量最大值、重复外部输入读取总量、每核总工作量最大值字典序选一份。其中必要 Pipe 工作量是下界，因为同核同 Pipe 必须串行，官方还可能增加 COPY、等待和竞争成本。下界大于已知官方最好时间时，可以不评分该候选；相等时仍保留改善次目标的可能。其余静态排序只是启发式，不保证所选布局优于未被选中的两个。

component_guard 在原三个初解后评分一次；component_slot 保持三个初解和早期基础粒度/定向搜索，在第一次普通随机扰动机会构造原提案并消耗原随机数后，替换为该分量候选。若未评分则恢复原随机提案，若评分则占同一预算机会；不直接替换基础粒度、定向拆分、局部重排或区域修复。结构候选只有在官方字典序目标改善时才加入动态成本观察，防止失败候选改变后续已有效的成本状态。后续父解仍可能变化，不能保证所有输入无退化。

两种位置固定做消融：原十图加 006/040/090/097 四图，2～5 核、种子 0；十图五核种子 1/2 稳健性；三种配置共 228 次。所有运行保持 12 次机会及独立原版官方复核。结果、正负结论与全量验证资格见 `审查/问题一分量预算优化验证_20260924.md`。正式采用算法与阶段不随这一原型自动改变。


## 9. 首次改善驱动的第二布局预算（实验配置）

component_followup 继承 component_slot 的路由、第一候选及官方字典序目标。只在首个分量候选经正式评分后严格降低当时最优 Makespan 时解锁另一份布局；只减少搬运不触发。第二份取剩余静态排名中节点分组不同者，忽略核心标签置换判断分组差异，最多生成三种布局、实际评分最多两种，不做组合枚举。

预算仍为 12 次机会。第二布局可替代后续普通随机提案，或者与某个已评价方案仅子图编号不同的重复提案；后者保留核心身份与核心执行顺序比较，记录被复用的评价编号。新颖的基础划分、定向拆分、重排及区域修复不被直接替换；如果没有这种机会，第二布局不评分。每份布局使用自身必要 Pipe 工作量下界剪枝，重复或剪枝时返回原提案。四档基础粒度仍构造。

这保护的是搜索机会类型，不是所有后续候选的字节内容：接受新布局后父解和局部观察可能改变，仍可能出现退步。结构评分继续只在字典序目标改善时写入局部观察。正式默认不变；实验规则见条件第二布局协议，结果由独立官方核验决定。


## 20260924 固定池局部排序与100图验证

采用 `component_local_rank` 实验配置，保持既有候选池、结构门槛和12次评价机会；用冻结的官方局部准备及近似任务依赖回放给最多三种完整分量布局排序。不把局部预测当全局下界，不把完整DDR仿真藏入代理；局部准备耗时单列。首次结构方案改善后才解锁结构不同的第二布局，保护四种基础粒度搜索。

18个固定候选池的平均相对选择遗憾由3.369965%降至0.079861%，前两名覆盖率由88.89%升至100%。14图同预算56对比较为10胜46平0负，总执行时间仅下降0.023081%；这才是本轮排序的独立收益。双种子20对为3胜17平0负，额外搬运略增0.000101%。

冻结后100图×2～5核全部完成并通过原版评估器最终复核，逐用例平均加速比为1、1.814349、2.481665、3.071797、3.545443。相对原全量shared_region基线197胜203平0负。5核总时间80792637→75956438 cycles，下降5.985940%；总额外搬运2708720606→2303561972 bytes，下降14.957565%。全量收益包含前几轮分量初解和预算改动，不能都归因于本轮排序。

参考工程5核平均加速比3.651513，仍高于本方案；但参考总时间78825871，本方案低3.640217%。两指标权重不同。参考的搜索预算不同，比较结果不证明同求解耗时优势。

下一步先核查case_058/039/072的并集内存筛选假阳性；并集不是活跃峰值。主导分量路由是净差距最大类，应检查case_009/100/040的有界大分量切分与小分量完整保留。维持大图028/067优势。4组搜索超过600秒，最长864.298秒（服务器16并发，不含单核冷计算及最终复核），仍未达到所有用例5～10分钟要求。

证据：`图表/runs/20260924-A-q1-ranking-analysis/report.md`、逐例CSV、完整source_snapshot及search/plan/verification。正式默认、main采用和人工门禁未改变。


## 20260924 内存路由、主导分量与精确收缩加速

本轮继承b69c237，分支codex/q1-memory-routing。沿用场景A：同核不同Task也清空缓存，所有跨子图数据均走DDR；不把同核放置当作免费通信。

- `component_memory` 对原先“所有分量并集超容量”路由提出最多三种布局（完整分量LPT/Pipe、工作量分批），局部排序选一，最多一次结构评分。058/039/072的12组目标配置12胜，合计执行时间下降14.6173%、额外搬运下降45.1633%。有真实溢出也可能通过减少通信和等待获得更短Makespan，不能仅因溢出而硬排除。
- `component_hybrid` 仅切分工作量超过平均每核份额的主导分量，保留小分量。沿拓扑顺序按Pipe累计工作量形成两种有界候选，每重分量最多2k块，不穷举边界。009/100/040的12组目标配置10胜2平，合计时间下降20.7129%、额外搬运下降81.2901%。两核040/100持平。
- 两类均保持12机会、11次完整评分+1次固定单核引用、四基础粒度。全部40份原版最终复核通过。028/067保护组：hybrid 8份方案完全一致；memory 7份一致，028四核退步0.16747%（10676362→10694242）。失败内存候选挤掉末尾一次有效local_reschedule，损失17880cycles，证明预算机会成本仍需处理。不得只报告目标图胜率。
- 精确运行优化`component_fast`以当前子图DAG可达性判断收缩是否成环，接受后增量更新；不改合并次序或候选预算。90项测试通过。091的2～5核搜索分别267.899/288.009/288.889/270.923秒，轨迹、最终方案和全部评价输出与已通过原版复核的历史结果相同。
- 五核单worker顺序配对493.714→167.232秒，观测2.952倍、耗时减少66.128%。仅一次配对，后台负载有变化；搜索计时不含固定单核冷计算及原版最终复核，不能说端到端都已低于10分钟。原始cProfile耗时不用于比较，749个采样定位重复成环检查，剖析运行中断日志保留。

本轮只有六目标图与两保护图，不是新的100图成绩。内存路由有保护组退步，暂不替换默认；两路由与精确加速的组合未正式跑全量，不能将各自收益相乘。正式入口、main与人工门禁不变。证据：图表/runs/20260924-A-q1-memory-analysis-v2/report.md及memory-routing全部原始结果。


## 20260924 新候选机会成本：局部筛选未通过

分支 `codex/q1-opportunity-budget`，输入 `6e48ee0`。对058/039/072、009/100/040及保护图028/067跑2～5核、seed0共32组，维持每组12次机会（11次完整评分+固定单核引用）、四基础粒度。按结构自动选择memory/hybrid路由；新筛选只允许同口径核内局部回放预测严格改善当前解的候选占用机会。它是代理，未调用额外全局评价，不能证明真实收益。

与上一轮对应未筛选路由配对：**0胜30平2负，总时间增加0.617833%，额外搬运增加3.604802%**。30次可配对筛选中，6次拒绝：4次正确拒绝无收益候选，2次误拒有收益候选；24次放行中4次未改善。039四核最终395323→564871；072两核11841342→12769723。028四核仍放行坏候选，最终10694242，比旧基线10676362慢17880cycles，未解决机会成本。

内存目标三图合计38564088→39662017，主导分量三图1074621→1074621；保护条件失败。预先选定的16图扩展未启动，不修改规则事后放宽门槛，不替换默认，不报告新的100图成绩。相对更旧全量基线的20胜11平1负不能用来掩盖相对上轮的退步。

93项测试通过，32份输入/代码/输出哈希及预算核查通过。本轮全局搜索352次，固定单核引用32次；32个最终方案与历史原版复核方案逐字节相同且完整评价输出一致，因此复用原版最终复核，未增加搜索预算。首次启动参数遗漏在计算前失败，日志保留；本地CLI冒烟单独计数。服务器16并行下最大搜索237.490秒，不含单核冷基准和原版复核，不能据此宣称全部大图端到端达标。

下一步先在固定候选池检查共享DDR并发导致的预测残差，并记录新候选挤掉的基础候选及其后续链条贡献；不按个别图结果调整阈值。新规则冻结且保护组通过后，才运行已预选的16图同预算对照。证据：`图表/runs/20260924-A-q1-opportunity-analysis-v2/report.md` 与 `20260924-A-q1-opportunity-budget`。


## 20260924 全局成本机制校准：候选准入与预算机会审计

本轮继承已有场景A模型及候选池，仅校准新memory/hybrid候选的准入比较。利用既有`q1_pipe_event`实现，把固定Task时长比较改为保留四Pipe顺序、算子依赖、内存复用依赖与Task间释放约束的三轮近似回放。非DDR算子的时长保持不变，DDR算子的估计时长按上一轮时间区间内平均带宽份额作半步松弛，再沿依赖路径传播延迟。该机制仍是有界启发式代理，非完整官方全局仿真；不拟合逐case系数，不承诺下界或精确预测。

准入规则为候选代理Makespan严格低于当前最好解的同口径代理Makespan。两边均重新准备核内依赖图，额外两次核内准备及代理耗时明确登记，全局完整评分调用为零。候选池内部排序保持上一轮局部排序不变，故本轮验证的是准入校准，不是全面更换排序器。

预算审计新增被替代方案hash、原动作、评分位置及当时是否已精确评分，随后与原基线的方案/动作hash对照。必须区分可行且已评分、重复、不可行及未匹配四类；不可行试探本来不消耗评分机会，替代它并不代表得到免费预算。后续缺席的有效动作只作为轨迹差异记录，不能把其增益当成可独立相加的因果贡献。

开发8图的冻结条件通过后才扩展至预先选定16图；保留seed0、2～5核、12次机会与四基础粒度。完整评分、固定单核引用、核内准备和最终独立复核分开记账。正式默认、main与人工门禁不变。

本轮实验证据：开发1胜31平、扩展64组全平；事件代理降低预测误差但新增执行时间收益有限。详细数值、预算机会审计与求解开销见计算结果.md的20260924全局成本机制校准段及对应v2报告。


## 20260924 同候选池事件排序与核内准备复用

继承上一轮场景A模型、三轮事件代理和候选生成规则，本轮只把候选池的排序指标从独立核内时长回放改为事件依赖代理Makespan；相同分数沿用生成序号，最多三种布局和结构多样性规则不变。不得把同核跨Task通信当作免费通信，不新增穷举或提高正式评价预算。全局评分仍负责最终接受与输出。

准备复用限定在单次求解实例内：输入与硬件配置固定，核内缓存键为含子图ID的完整算子划分，事件分数缓存键为含核心顺序的完整计划。划分缓存最多2份、分数缓存最多8份，命中后隔离可变对象；核心映射、Task前驱仍重新验证并应用。复用准备不复用全局评分，每次正式评分仍执行完整全局事件过程并计预算。

首版把所有完整评分产生的新划分复制入缓存，实际出现额外开销。V2只保存局部排序准备的划分，完整评分未命中时直接构建，不保存一次性大对象；首次准备只复制用于保存的一份，不再次复制返回对象。该修正不改变候选和评分结果。100项测试通过；009/040/100在2、4核的6组串行对照及009两核跨平台检查，计划、完整官方结果与搜索轨迹相同。串行对照求解耗时43.7920→42.7718秒，观测减少2.3297%，属于小样本工程验证，不代表全量端到端耗时。

算法效果由8图开发、16图扩展分别比较；规则通过后再按同一版本运行100图400配置。由于V2改变源码哈希，旧版本验证结果不直接拼入全量，全部400配置重新计算，历史原版最终验证仅按严格同方案同结果条件复用。该全量运行现已完成并通过核验，平均加速比及对照见计算结果.md和全量报告。

事件排序的24图验证已经完成：开发4胜26平2负、扩展6胜56平2负，虽然总Makespan分别下降0.030126%和0.089807%，两个阶段的三核逐图平均加速比均退步，额外搬运分别增加0.229879%和1.109579%，未通过预定条件。不能因大图节省的cycles掩盖小图或特定核数的加速比损失。按预案固定回退为`routes_gate_reuse`：原局部池排序＋事件准入＋V2准备复用。

回退版本的6组串行配对与旧准入版本逐项等价，另有一次跨平台等价检查；观测累计搜索耗时仅减少0.398125%，不能认为是显著性能提升。该机制主要避免重复准备，效果须由全量真实运行报告；所有正式评分仍计预算。V1验证129次、V2两种策略的等价探针26次均独立保留，额外搜索评分1705次，不混入最终400配置的4400次正式搜索评分。全量固定30个小图本机、70个图服务器，无逐例择优。


全量结果：1～5核平均加速比分别为1、1.834917、2.546238、3.161009、3.665903。相对上一版全量68胜330平2负，五核总时间下降3.667809%、额外搬运下降23.261093%；这包含前序结构路由等机制的组合收益。最长搜索363.859秒，不含单核冷生成及最终独立复核。两核和五核平均指标略胜参考、三核和四核略低，额外搬运仍存在差距。全量记录与审计均已落盘，未自动变更正式默认或main。
