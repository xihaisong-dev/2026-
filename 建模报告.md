# 问题一建模与算法交接

> **2026-09-24 当前采用版本：r02 / routes_gate_reuse。** 本文保留研发历史，写作以 `审查/问题一交付与赛题要求核对_r02.md` 和 `协作/交接/Q1/Q1-WRITE-r02.md` 为当前入口。单图运行 `python 程序/q1_submit.py 数据/processed/q1/data/case_001.json -n 5 --output trial --verify-final`；批量运行 `python 程序/q1_reproduce.py --output all100 --workers 2`。输入尚未导入时先运行 `python 程序/主程序.py prepare`。当前程序包 `output/q1-delivery-r02-20260924-portable.zip`，校验后再运行。1～5核平均加速比：1、1.834917、2.546238、3.161009、3.665903。只报告我们自身工程；旧历史段落不是当前提交口径。


本报告描述已实现的研究版本，供问题一章节写作。正式工作流状态仍以 `状态/工作流状态.json` 为准，不据此宣称阶段完成。

模型定义 Q1 | academic_name: 考虑共享DDR与缓存约束的有向无环图划分—多处理器调度组合优化模型 | canonical_model_family: 有向无环图划分与带前驱约束的多处理器调度组合优化

模型结构 Q1 | solver_algorithm: 结构感知初始划分、关键路径调度与预算保护的有界启发式搜索

论文表达 Q1 | 展示名称: 共享DDR约束下的图划分与多核调度模型 | 问题角色: new_model | 继承模型: none | 核心方法: 完整分量与主导分量初解、局部成本排名及事件准入、预算保护的有界修复

## 1. 决策与目标

给定算子有向无环图 G=(V,E)，将非 COPY 操作集合 Vc 划分为互不相交且并集为 Vc 的子图 P={V1,...,Vq}；每个子图构成一个 Task。决策由划分 π、核心映射 m 和各核顺序 σ 组成。核心数 k∈{2,3,4,5}。商图中的依赖边与各核串行顺序边合并后必须无环；每个操作恰好属于一个子图，每个 Task 恰好分配一个核。

采用字典序目标 min (T(π,m,σ), B_add(π,m,σ))：首先最小化总完成时间，时间相同时选择额外搬运较少者。T 由赛题事件评估过程 E(G,π,m,σ;k,config) 给出，等于所有 Task 结束时刻的最大值。B_add 等于调度后 COPY 总字节数减去原图 COPY 字节数，包括划分额外搬运与核内缓存溢出搬运。这里是黑箱可计算的组合优化目标，不声称构建了可由 MILP 求解器精确求解的线性模型。

## 2. 时间、资源与通信约束

对于依赖 Task u→v，v 的释放时间不得早于 u 完成加跨核同步延迟；本配置跨核为 1000 cycles、同核数据前驱不另加该跨核等待。同一核的前一 Task 完成后还需要 100 cycles 的任务间隔。多个释放条件取最大值，不把所有前驱等待求和，也不把每条跨核边的 1000 直接加到目标函数。

同核连续执行不保留私有缓存，所以跨 Task 张量仍须经过 DDR。固定配置的 DDR 带宽为 60 bytes/cycle，L1 容量 524288 bytes，UB 容量 131072 bytes。容量不足通过官方核内调度插入的换入换出处理；各 Pipe 的操作顺序、数据依赖、内存复用依赖以及共享 DDR 的动态竞争共同决定事件开始与结束。四条 Pipe 可以在满足依赖时并行，不能把各自工作量相加当作 Task 时间。

最终评价使用附件官方语义。搜索代理则使用近似成本与已观察的局部核内时长，服务于候选筛选；代理误差尤其可能来自共享 DDR 和释放链变化。局部精确时长不等于全局精确 Makespan。任何执行完整全局仿真的候选检查都计入正式评价机会，不包装成免费代理。

## 3. 已采用算法及其理由

入口为 `程序/q1_submit.py`，实际搜索在 `程序/q1_experimental.py`。采用 shared_region，启用 local_cost、critical、insertion、comm_rank、partition_guard、shared_input、region 七项机制，预算 12、种子 0。代码中其他实验开关的存在不代表本轮结果使用了它们。尤其不能把 adaptive/portfolio/budget_adapt 或 region_gap 写成当前已经启用的特性。

1. 先评价整图单 Task 的保底方案，再生成拓扑贪心划分；优先利用输入复用与依赖关系，控制子图大小。其计算量低、易于解释，为后续提供合法初解。
2. 对合法商图作有界多层次无环粗化，再逐层展开和比较。共享输入信息用于识别仅看两两边权容易漏掉的重复读取代价。该模块借鉴多层次思想，不宣称完整复现某篇有向超图论文的全部算法。
3. 用关键路径优先级、通信相关排序与最早完成时间选择核心。基础映射支持合法空隙插入，同时满足两侧任务间隔；这是减少无谓排队的调度启发式，并非穷举所有顺序。
4. 保护基础粒度划分的搜索机会，再进行有限的划分、映射和顺序候选调整。关键区域修复选择至多 4 个关联 Task、至多 512 个操作；至多 6 组映射与 2 种优先级构造有限代理候选，再返回至多 2 个不同方案。区域外核心归属与相对顺序保持约束，所有候选验证合法性和无环性。本轮 region_gap 关闭，不能把一般映射的空隙插入误写成区域修复器也启用了该开关。
5. 最终接受由官方目标决定，保留字典序最优解。评价机会包含整图保底和命中缓存的候选，缓存不自动增加预算；候选产生尝试也有上限，不遍历所有划分、核心组合或任务排列。

选择这条路线，是因为合并减少重复搬运却会损失并行度，过度切分增加 DDR 与任务释放成本，单纯最小割或均分操作数都不能直接保证 Makespan 更好。多种初解探索不同粒度，局部联合修复改变少量相关决策，最终由统一评估处理非线性的带宽与缓存影响。在有限时间下，这比组合穷举可执行，也比只优化代理分数可靠；仍不保证全局最优。

研究初期讨论过 ALNS 等路线，仓库也保留实验实现。但当前全量采用方案不等于所有自适应机制全开。具体机制必须以冻结配置及源代码为准。

## 4. 全量实验与最优算例

采用固定单核基准定义 S_ik=T_i1/T_ik，平均值为 (1/100)∑S_ik。1～5 核平均加速比分别为 1、1.700389、2.264697、2.685511、2.990661。已交付 100 图×5 核共 500 条采用结果；对照另有 400 条。

以相对自身单核的加速比衡量，2～5 核的最高值均出现在 case_097：2.190063、3.274339、4.247084、5.273640；对应 Makespan 为 5247114、3509566、2705741、2179047 cycles，固定单核为 11491509 cycles。1 核全部算例加速比均为 1，无唯一第一。

case_097 的画像显示较高计算单元占用与较低关键链释放等待；单核基准还存在启发式导致的溢出，多核划分后的溢出为零，因此超过核数的加速比并不违反题意。但不能把所有收益因果归结为消除溢出：其单核额外搬运 10584064 bytes，多核额外搬运约 85295104～92076032 bytes，划分搬运反而更多。此例体现主要目标和次级目标的权衡。不同图原始工作量不同，绝对最短 Makespan 不是这里的“最优算例”定义。

## 5. 求解计算时间的优化与边界

`程序/q1_fast_evaluator.py` 在官方源码哈希锁定下，以剩余操作计数、活跃 Task 集合、张量反向索引和分 Task 时间线索引替代重复全表扫描，保持官方步骤、事件顺序及带宽运算。`程序/q1_fast_costs.py` 缓存固定成员顺序对应的基础成本，动态局部观测仍使用当前状态；缓存不能把已校准的旧状态带入新候选。

验证范围：500 个保存方案的完整评估输出逐项一致，500 个划分的基础成本冷/热缓存一致；原先耗时超过 600 秒的 6 个配置完整重跑搜索，候选序列、最终方案及官方结果一致。6 个配置的加速后搜索加固定单核计算为约 110～281 秒。另一次同机串行 case_084 单核评估从 18.354 秒到 2.917 秒，约 6.29 倍。这是特定评估调用的运行加速，不是任务执行加速；不能将不同机器、并发条件下的旧耗时与新耗时相除。尚未在新后端重跑全部 400 个搜索，不能宣称全部未来用例必定在 5 分钟内完成。

后续先测剩余耗时的组成，再优化重复图构建、局部拓扑处理与有完整输入/配置/评估器键的复用；固定单核结果每图仅算一次，采用内存约束下的进程并行。对冷/热缓存、串行/并行分别报告墙钟时间与峰值内存。若引入硬截止时间，需重新比较同预算质量与超时比例，不能悄悄减少候选后沿用当前质量结论。

## 6. 写作证据与限制

数值来源为 `图表/runs/20260923-A-q1-v18-merged/` 与 `...v18-analysis/`；写作五点与逐例表由 `程序/q1_writing_summary.py` 重建。结果一致性见 `...runtime-final-all100/`、`...runtime-cost-check/`、`...runtime-slowest-six/`、`...runtime-final-paired/`。算法符合性与复现交付分别见 `审查/问题一原基线赛题符合性审核_20260923.md`、`审查/问题一复现交付与大图运行加速_20260923.md`。

论文应报告局部代理误差、固定种子、启发式基准、有限预算与全量新后端搜索尚未重跑等限制；不声称最优性、90% 时间下降、全部新机制有效或正式提交审核已通过。文献引用须由写作者根据原文核实，不能由算法名称推断本实现就是某文献的完整算法。


## 7. 结构初始候选的独立验证

新增 `seed_components`、`seed_batches`、`seed_depth` 和 `seed_combined` 四个实验配置，不更改正式采用的 `shared_region`。目的是解决固定粒度初解可能遗漏的结构：多个相互独立分量可在核间并行，单个大分量则需要合法切分才能释放并行性。

首先去除输入输出 COPY 节点对计算任务归属的干扰，保留其传递的计算依赖，再求计算 DAG 的弱连通分量。完整分量分核用 LPT、按 Pipe 工作量均衡及轮转三种有界策略，把同核分量合成一个 Task；这些分量之间没有计算依赖，但仍可能重复读取同一 DDR 输入，且合并后可能增加缓存压力，不能当作免费的并行。分量分批保留每个分量完整性，以 256 个计算操作为目标批量，限制合并范围；该数量是粒度参数，不是内存容量约束。只有分量数量不少于两倍核数、且最大分量工作量不超过均摊工作量的 1.15 倍时才生成分批候选。

若最大分量超过该阈值，则生成宽度 8、16、4 的单调拓扑深度窗口。每个窗口内再次求弱连通分量，形成无环商图；按尾部关键路径优先级和估计完成时间选择核心及追加顺序。深度随有向边单调增长，组间依赖只向后，拓扑追加核心顺序不会引入调度环。估计搬运成本在同核跨 Task 时也计入 DDR，不假设跨 Task 私有缓存驻留。成本模型只是候选构造依据，最终接受仍由统一官方场景 A 评价决定。

新候选放在既有三个初解之后，规范化子图标签后去重，最多占四个评价机会，同时预留四档基础粒度尝试与后续搜索机会。每次求解仍为 12 个评价机会，包括固定单核结果复用一次。保护的是基础粒度的尝试额度，不保证后续候选与基线完全相同，因为新评价会改变成本观察与当前父解。应同时分析预算挤占和搜索路径改变。

对照采用原五图 017/045/048/065/077 加反例 002/028/063/067/085，2～5 核、种子 0，五方案共 200 次；另预先冻结种子 1/2 的五核基线及组合共 40 次。逐方案官方核验、配对结果和采用建议见 `审查/问题一结构初解同预算验证_20260923.md`。这十图是开发验证集，不是全量或盲测成绩。方法生成部分改编自用户提供工程，来源哈希见代码和实验协议，不能声称为原创算法。


## 8. 完整分量候选与结构预算控制（20260924 原型）

本轮继承问题一原模型，不新增目标或改变场景 A 的 DDR、同步与缓存语义。以完整计算弱分量为原子单位，只尝试一份静态筛选的布局，避免将外部工程全部候选追加进已有 12 次评价预算。

每个分量的权重取四条 Pipe 操作周期和的最大值。分量数量不足核数，或最大分量权重超过总权重/核数时，继续使用原算法；若每个分量的内部张量并集都超过某一私有存储容量，也保守地使用原算法。并集并不是活跃峰值，这不是合法性判断，只是避免在典型大工作集上盲目融合的路线规则。没有使用 case 编号或大图节点数量阈值。

布局候选最多为完整分量 LPT、Pipe 平衡与轮转三种，去重后按每核必要 Pipe 工作量最大值、重复外部输入读取总量、每核总工作量最大值字典序选一份。其中必要 Pipe 工作量是下界，因为同核同 Pipe 必须串行，官方还可能增加 COPY、等待和竞争成本。下界大于已知官方最好时间时，可以不评分该候选；相等时仍保留改善次目标的可能。其余静态排序只是启发式，不保证所选布局优于未被选中的两个。

component_guard 在原三个初解后评分一次；component_slot 保持三个初解和早期基础粒度/定向搜索，在第一次普通随机扰动机会构造原提案并消耗原随机数后，替换为该分量候选。若未评分则恢复原随机提案，若评分则占同一预算机会；不直接替换基础粒度、定向拆分、局部重排或区域修复。结构候选只有在官方字典序目标改善时才加入动态成本观察，防止失败候选改变后续已有效的成本状态。后续父解仍可能变化，不能保证所有输入无退化。

两种位置固定做消融：原十图加 006/040/090/097 四图，2～5 核、种子 0；十图五核种子 1/2 稳健性；三种配置共 228 次。所有运行保持 12 次机会及独立原版官方复核。结果、正负结论与全量验证资格见 `审查/问题一分量预算优化验证_20260924.md`。正式采用算法与阶段不随这一原型自动改变。


## 9. 首次改善驱动的第二布局预算（实验配置）

component_followup 继承 component_slot 的路由、第一候选及官方字典序目标。只在首个分量候选经正式评分后严格降低当时最优 Makespan 时解锁另一份布局；只减少搬运不触发。第二份取剩余静态排名中节点分组不同者，忽略核心标签置换判断分组差异，最多生成三种布局、实际评分最多两种，不做组合枚举。

预算仍为 12 次机会。第二布局可替代后续普通随机提案，或者与某个已评价方案仅子图编号不同的重复提案；后者保留核心身份与核心执行顺序比较，记录被复用的评价编号。新颖的基础划分、定向拆分、重排及区域修复不被直接替换；如果没有这种机会，第二布局不评分。每份布局使用自身必要 Pipe 工作量下界剪枝，重复或剪枝时返回原提案。四档基础粒度仍构造。

这保护的是搜索机会类型，不是所有后续候选的字节内容：接受新布局后父解和局部观察可能改变，仍可能出现退步。结构评分继续只在字典序目标改善时写入局部观察。正式默认不变；实验规则见条件第二布局协议，结果由独立官方核验决定。


## 20260924 固定池局部排序与100图验证

采用 `component_local_rank` 实验配置，保持既有候选池、结构门槛和12次评价机会；用冻结的官方局部准备及近似任务依赖回放给最多三种完整分量布局排序。不把局部预测当全局下界，不把完整DDR仿真藏入代理；局部准备耗时单列。首次结构方案改善后才解锁结构不同的第二布局，保护四种基础粒度搜索。

18个固定候选池的平均相对选择遗憾由3.369965%降至0.079861%，前两名覆盖率由88.89%升至100%。14图同预算56对比较为10胜46平0负，总执行时间仅下降0.023081%；这才是本轮排序的独立收益。双种子20对为3胜17平0负，额外搬运略增0.000101%。

冻结后100图×2～5核全部完成并通过原版评估器最终复核，逐用例平均加速比为1、1.814349、2.481665、3.071797、3.545443。相对原全量shared_region基线197胜203平0负。5核总时间80792637→75956438 cycles，下降5.985940%；总额外搬运2708720606→2303561972 bytes，下降14.957565%。全量收益包含前几轮分量初解和预算改动，不能都归因于本轮排序。

参考工程5核平均加速比3.651513，仍高于本方案；但参考总时间78825871，本方案低3.640217%。两指标权重不同。参考的搜索预算不同，比较结果不证明同求解耗时优势。

下一步先核查case_058/039/072的并集内存筛选假阳性；并集不是活跃峰值。主导分量路由是净差距最大类，应检查case_009/100/040的有界大分量切分与小分量完整保留。维持大图028/067优势。4组搜索超过600秒，最长864.298秒（服务器16并发，不含单核冷计算及最终复核），仍未达到所有用例5～10分钟要求。

证据：`图表/runs/20260924-A-q1-ranking-analysis/report.md`、逐例CSV、完整source_snapshot及search/plan/verification。正式默认、main采用和人工门禁未改变。


## 20260924 内存路由、主导分量与精确收缩加速

本轮继承b69c237，分支codex/q1-memory-routing。沿用场景A：同核不同Task也清空缓存，所有跨子图数据均走DDR；不把同核放置当作免费通信。

- `component_memory` 对原先“所有分量并集超容量”路由提出最多三种布局（完整分量LPT/Pipe、工作量分批），局部排序选一，最多一次结构评分。058/039/072的12组目标配置12胜，合计执行时间下降14.6173%、额外搬运下降45.1633%。有真实溢出也可能通过减少通信和等待获得更短Makespan，不能仅因溢出而硬排除。
- `component_hybrid` 仅切分工作量超过平均每核份额的主导分量，保留小分量。沿拓扑顺序按Pipe累计工作量形成两种有界候选，每重分量最多2k块，不穷举边界。009/100/040的12组目标配置10胜2平，合计时间下降20.7129%、额外搬运下降81.2901%。两核040/100持平。
- 两类均保持12机会、11次完整评分+1次固定单核引用、四基础粒度。全部40份原版最终复核通过。028/067保护组：hybrid 8份方案完全一致；memory 7份一致，028四核退步0.16747%（10676362→10694242）。失败内存候选挤掉末尾一次有效local_reschedule，损失17880cycles，证明预算机会成本仍需处理。不得只报告目标图胜率。
- 精确运行优化`component_fast`以当前子图DAG可达性判断收缩是否成环，接受后增量更新；不改合并次序或候选预算。90项测试通过。091的2～5核搜索分别267.899/288.009/288.889/270.923秒，轨迹、最终方案和全部评价输出与已通过原版复核的历史结果相同。
- 五核单worker顺序配对493.714→167.232秒，观测2.952倍、耗时减少66.128%。仅一次配对，后台负载有变化；搜索计时不含固定单核冷计算及原版最终复核，不能说端到端都已低于10分钟。原始cProfile耗时不用于比较，749个采样定位重复成环检查，剖析运行中断日志保留。

本轮只有六目标图与两保护图，不是新的100图成绩。内存路由有保护组退步，暂不替换默认；两路由与精确加速的组合未正式跑全量，不能将各自收益相乘。正式入口、main与人工门禁不变。证据：图表/runs/20260924-A-q1-memory-analysis-v2/report.md及memory-routing全部原始结果。


## 20260924 新候选机会成本：局部筛选未通过

分支 `codex/q1-opportunity-budget`，输入 `6e48ee0`。对058/039/072、009/100/040及保护图028/067跑2～5核、seed0共32组，维持每组12次机会（11次完整评分+固定单核引用）、四基础粒度。按结构自动选择memory/hybrid路由；新筛选只允许同口径核内局部回放预测严格改善当前解的候选占用机会。它是代理，未调用额外全局评价，不能证明真实收益。

与上一轮对应未筛选路由配对：**0胜30平2负，总时间增加0.617833%，额外搬运增加3.604802%**。30次可配对筛选中，6次拒绝：4次正确拒绝无收益候选，2次误拒有收益候选；24次放行中4次未改善。039四核最终395323→564871；072两核11841342→12769723。028四核仍放行坏候选，最终10694242，比旧基线10676362慢17880cycles，未解决机会成本。

内存目标三图合计38564088→39662017，主导分量三图1074621→1074621；保护条件失败。预先选定的16图扩展未启动，不修改规则事后放宽门槛，不替换默认，不报告新的100图成绩。相对更旧全量基线的20胜11平1负不能用来掩盖相对上轮的退步。

93项测试通过，32份输入/代码/输出哈希及预算核查通过。本轮全局搜索352次，固定单核引用32次；32个最终方案与历史原版复核方案逐字节相同且完整评价输出一致，因此复用原版最终复核，未增加搜索预算。首次启动参数遗漏在计算前失败，日志保留；本地CLI冒烟单独计数。服务器16并行下最大搜索237.490秒，不含单核冷基准和原版复核，不能据此宣称全部大图端到端达标。

下一步先在固定候选池检查共享DDR并发导致的预测残差，并记录新候选挤掉的基础候选及其后续链条贡献；不按个别图结果调整阈值。新规则冻结且保护组通过后，才运行已预选的16图同预算对照。证据：`图表/runs/20260924-A-q1-opportunity-analysis-v2/report.md` 与 `20260924-A-q1-opportunity-budget`。


## 20260924 全局成本机制校准：候选准入与预算机会审计

本轮继承已有场景A模型及候选池，仅校准新memory/hybrid候选的准入比较。利用既有`q1_pipe_event`实现，把固定Task时长比较改为保留四Pipe顺序、算子依赖、内存复用依赖与Task间释放约束的三轮近似回放。非DDR算子的时长保持不变，DDR算子的估计时长按上一轮时间区间内平均带宽份额作半步松弛，再沿依赖路径传播延迟。该机制仍是有界启发式代理，非完整官方全局仿真；不拟合逐case系数，不承诺下界或精确预测。

准入规则为候选代理Makespan严格低于当前最好解的同口径代理Makespan。两边均重新准备核内依赖图，额外两次核内准备及代理耗时明确登记，全局完整评分调用为零。候选池内部排序保持上一轮局部排序不变，故本轮验证的是准入校准，不是全面更换排序器。

预算审计新增被替代方案hash、原动作、评分位置及当时是否已精确评分，随后与原基线的方案/动作hash对照。必须区分可行且已评分、重复、不可行及未匹配四类；不可行试探本来不消耗评分机会，替代它并不代表得到免费预算。后续缺席的有效动作只作为轨迹差异记录，不能把其增益当成可独立相加的因果贡献。

开发8图的冻结条件通过后才扩展至预先选定16图；保留seed0、2～5核、12次机会与四基础粒度。完整评分、固定单核引用、核内准备和最终独立复核分开记账。正式默认、main与人工门禁不变。

本轮实验证据：开发1胜31平、扩展64组全平；事件代理降低预测误差但新增执行时间收益有限。详细数值、预算机会审计与求解开销见计算结果.md的20260924全局成本机制校准段及对应v2报告。


## 20260924 同候选池事件排序与核内准备复用

继承上一轮场景A模型、三轮事件代理和候选生成规则，本轮只把候选池的排序指标从独立核内时长回放改为事件依赖代理Makespan；相同分数沿用生成序号，最多三种布局和结构多样性规则不变。不得把同核跨Task通信当作免费通信，不新增穷举或提高正式评价预算。全局评分仍负责最终接受与输出。

准备复用限定在单次求解实例内：输入与硬件配置固定，核内缓存键为含子图ID的完整算子划分，事件分数缓存键为含核心顺序的完整计划。划分缓存最多2份、分数缓存最多8份，命中后隔离可变对象；核心映射、Task前驱仍重新验证并应用。复用准备不复用全局评分，每次正式评分仍执行完整全局事件过程并计预算。

首版把所有完整评分产生的新划分复制入缓存，实际出现额外开销。V2只保存局部排序准备的划分，完整评分未命中时直接构建，不保存一次性大对象；首次准备只复制用于保存的一份，不再次复制返回对象。该修正不改变候选和评分结果。100项测试通过；009/040/100在2、4核的6组串行对照及009两核跨平台检查，计划、完整官方结果与搜索轨迹相同。串行对照求解耗时43.7920→42.7718秒，观测减少2.3297%，属于小样本工程验证，不代表全量端到端耗时。

算法效果由8图开发、16图扩展分别比较；规则通过后再按同一版本运行100图400配置。由于V2改变源码哈希，旧版本验证结果不直接拼入全量，全部400配置重新计算，历史原版最终验证仅按严格同方案同结果条件复用。该全量运行现已完成并通过核验，平均加速比及对照见计算结果.md和全量报告。

事件排序的24图验证已经完成：开发4胜26平2负、扩展6胜56平2负，虽然总Makespan分别下降0.030126%和0.089807%，两个阶段的三核逐图平均加速比均退步，额外搬运分别增加0.229879%和1.109579%，未通过预定条件。不能因大图节省的cycles掩盖小图或特定核数的加速比损失。按预案固定回退为`routes_gate_reuse`：原局部池排序＋事件准入＋V2准备复用。

回退版本的6组串行配对与旧准入版本逐项等价，另有一次跨平台等价检查；观测累计搜索耗时仅减少0.398125%，不能认为是显著性能提升。该机制主要避免重复准备，效果须由全量真实运行报告；所有正式评分仍计预算。V1验证129次、V2两种策略的等价探针26次均独立保留，额外搜索评分1705次，不混入最终400配置的4400次正式搜索评分。全量固定30个小图本机、70个图服务器，无逐例择优。


全量结果：1～5核平均加速比分别为1、1.834917、2.546238、3.161009、3.665903。相对上一版全量68胜330平2负，五核总时间下降3.667809%、额外搬运下降23.261093%；这包含前序结构路由等机制的组合收益。最长搜索363.859秒，不含单核冷生成及最终独立复核。两核和五核平均指标略胜参考、三核和四核略低，额外搬运仍存在差距。全量记录与审计均已落盘，未自动变更正式默认或main。


## 问题二：跨子图驻留与共享 DDR 约束下的资源约束调度模型

模型定义 Q2 | 身份：Q1 的场景B扩展 | 标准模型族：资源约束项目调度（RCPSP）扩展 | 计算实现：离散划分/映射/顺序搜索及官方事件评价。
模型结构：决策为原计算操作到子图的映射 s(v)、子图到核心的映射 c(s)、每核子图优先序 pi。操作开始时间、COPY、spill 和物理张量实例由固定官方构造及调度规则诱导，不能当作可随意提交的独立变量。
论文表达 Q2 | 展示名称: 跨子图驻留与共享DDR约束下的资源约束调度模型 | 问题角色: model_extension | 继承模型: Q1 | 核心方法: 结构感知初解、通信亲和分核、操作级瓶颈引导的有界联合修复。

主要目标 min T，T=max_v finish(v)；报告 A=scheduled_copy_bytes-original_graph_copy_bytes。同周期少搬运是本轮显式求解约定，不自设加权目标。给定张量t，大小b_t，生产核集合P_t、消费核集合C_t：内部跨核基准字节为 2 b_t sum_{p in P_t,c in C_t}1[p!=c]；无计算生产者的图输入计 b_t|C_t|；图输出按生产核保留写回。直接Op边逐边处理，溢出搬运另由官方规则统计。不能按消费操作数量重复计费，不能把所有同核子图边计作DDR。

约束包括原图依赖、完整且唯一分配、核内序列与依赖相容、生成的操作级/FIFO/内存依赖图可执行。跨核 COPY_IN 的最早发射不早于源 COPY_OUT 完成+500；无额外100/1000 Task等待。DDR总带宽60 bytes/cycle，所有活跃DDR传输共同分配，不能用每核独享60估算最终成绩。各核每种Pipe按固定队列串行、不同Pipe可重叠。

对每核每存储层r，在任何时刻tau，所有实际物理实例的驻留字节和不超过Cap_r。实例从官方分配事件持续到最后使用完成的释放事件，spill/reload产生不同实例，跨子图不自动清空。Cap_L1=524288、Cap_UB=131072 bytes；不得使用 Q3 的共享L2。依赖、容量、同步、DDR竞争最终统一由未修改的官方B验证。

继承Q1的图结构与预算工程，但重建B的成本：按照增量消费核心通信生成放置候选，再用官方B评价。实际关键图采用已观测操作时长，只解释固定运行；队首阻塞机会用于排序提案，不是反事实时间节省保证。驻留面积只是诊断，不作为替代T的主目标。普通/引导J均保护8个基础机会，追加相同4机会；新机制不能挤占既有有效搜索。缓存版本边界与首轮样本详见问题分析.md及冻结contract.json。

预处理合同：继续使用数据/processed/q1内逐字节审计的同一附件输入，不修改图、算子、容量、带宽或官方源码；复用目录名不代表复用场景A成绩。全部旧Q2成绩只作历史证据，当前Q1 r02计划必须重新在B下运行。


## 问题二r04：合规核查与结构准入对照

继承既有B模型，仅修改求解器的候选准入，不建立新模型。模型硬约束由未修改官方B评价；HEFT的core_time等只是候选代理，不能解释为核内真实串行约束。题面正式输入为Op-Tensor二部图，兼容Op-Op边的测试代码不是题目扩展。

运行前固定路由：最大计算弱连通分量占比超过50%时，在8个基础机会中保护前6个，仅替换第7/8个为参考HEFT同划分候选；否则全部采用旧候选。再保留4次普通J，总12次机会。保护部分机会不保证旧最终成绩单调不退，必须逐例检验。新确认022/024/035用于补充上一轮未覆盖的单分量和主导分量结构。全量交付未完成，不宣称问题二全面验收；详细矩阵见审查/问题二建模解算要求审核_r04.md。


## 问题二r05采用边界

本轮统一采用算法为routed，数值由100图同预算实验决定。仍继承既有场景B模型。实际求解使用结构初解和普通有界J；结构准入仅按预冻结条件选择是否采用。上文关键链引导、修正通信代理、生命周期面积是研究机制与诊断，不代表全部进入最终算法，不能写成已验证的收益来源。题面正式输入不含Op-Op边。图表/全部结果.json的q2_full_r05为当前计算证据；不改Q1模型及成绩。


## 问题二 r07 当前采用边界

依据用户决定，当前计算交付升级为r07：沿用同一场景B模型、结构准入和普通有界J，保留基础有效候选并回收重复机会；总B机会仍12次。不是另立模型，也没有采用全部研究开关或r08共享准备。官方100图13胜387平0负，搬运总量增加0.028884%；证据见审查/问题二r07计算提交与复现.md。Q1模型、源码和冻结成绩不变。

## 问题三：共享FIFO缓存与双带宽池的多处理器调度扩展

模型定义 Q3 | 正式名称: 共享FIFO缓存约束下的多处理器调度离散事件优化模型 | 标准模型族: 多处理器调度与离散事件仿真优化 | 求解算法: Q2种子保护的缓存事件引导有界邻域搜索
模型结构 Q3 | 决策变量/状态量: 操作子图归属、子图核映射、核内序、FIFO条目序列、DDR与L2在途工作量 | 目标函数/统计关系: 周期优先且同周期少逻辑额外搬运的字典序最小化 | 核心约束/方程: 继承Q2全部依赖、Pipe、容量与500周期释放约束，加共享FIFO容量及独立公平带宽池 | 定制机制: 发射查询、完成填充、并发冷未命中和命中不刷新
论文表达 Q3 | 展示名称: 共享缓存约束的多处理器调度模型 | 问题角色: model_extension | 继承模型: Q2 | 核心方法: 缓存事件引导邻域搜索

### 决策、状态与目标

继承Q2的映射x、分核y与每核拓扑可行序π。给定方案p=(x,y,π)，官方确定性核内展开产生操作集及其内存依赖。新增参数C为L2容量、bL为L2带宽；Q(τ)为按插入先后排列的缓存逻辑张量序列，u(τ)为占用；每个搬运i的s_i、a_i、f_i分别表示字节数、发射及完成时刻。h_i表示其发射时是否命中；A_D、A_L表示两带宽池活跃集合。

$$
\min_{p\in\mathcal P_B}\operatorname{lex}\left(T_C(p),D_{\rm add}(p)\right),\qquad T_C(p)=\max_i f_i.
$$

此为离散事件评估定义的组合优化，非线性FIFO路径与事件排序不伪装成已求解的MILP。可行域继承Q2：节点覆盖、sgid唯一调度、子图DAG、同核合法顺序、四Pipe串行、L1/UB物理实例容量以及跨核COPY_OUT完成后500周期的释放。L2命中不能绕过同步或Pipe。

### FIFO与带宽状态方程

$$
 h_i=\mathbf1\{i\text{为正字节COPY\_IN},\ \ell_i\in Q(a_i^-)\},\qquad
 u(\tau)=\sum_{t\in Q(\tau)}s_t\le C.
$$

零字节操作不纳入命中率。COPY_IN完成时，若逻辑id已在缓存中则不改变队列；否则当s_i≤C时逐个淘汰队首直至可插入，再从队尾加入。超过容量者旁路。严格依官方实现：完成处理对所有COPY_IN调用插入，因此某次命中在飞期间若被淘汰，完成时亦可能重新加入。相同时间的完成与发射处理顺序保持官方确定性，不能凭连续流近似改写。

每条进入池r的搬运初始工作量是官方按独占带宽取整后的周期w_i=max(1,ceil(s_i/b_r))，零字节及核内搬运依官方规则处理。固定活跃集合区间内：

$$
\frac{d w_i(\tau)}{d\tau}=-\frac{1}{|A_r(\tau)|},\quad i\in A_r(\tau),\qquad A_D\cap A_L=\varnothing.
$$

到达、完成时结算剩余工作，重新预测完成并按官方整数时刻上取整。DDR与L2各自公平分配，不共享60+250的统一池，也不能把250当每核独占带宽。

### 指标推导及机制解释

$$
H=\frac{\sum_i h_i s_i}{\sum_{i\in COPY\_IN,\ s_i>0}s_i},\qquad
D_{DDR}^{derived}=D_{scheduled}-\sum_i h_i s_i.
$$

后式仅用于本题构图下DDR或L2服务的COPY；验证时排查ON_CHIP路径。官方D_add仍等于D_scheduled−D_original，相同计划的该字段不会因命中减少。H高未必T低：可能计算或同步关键链占主导，也可能L2命中搬运竞争。粗下界max(DDR服务工作量、L2服务工作量、每核每Pipe固定计算工作量、依赖链)仅供诊断，不当作可达最优值。

### 求解合同与消融

固定Q2 r07为保底，首先官方L2评价。利用其真实未命中事件，将多次未命中的逻辑张量映射到使用子图，优先尝试同核合法插入与跨核合法迁移，以改变首次填充及后续读取的相位；另保留Q2普通关键度邻域作为对照。所有候选保持图结构合法，均由原始官方Q3评价，不能以命中率代理替代Makespan。计划重复只记录、不额外占用真实评分；记录候选上限及实际调用。接受严格字典序改善；无改善保留Q2种子。单核同样允许合法子图次序优化，但整图无可移动子图时自然退化。

不得把先前Q2搜索成本藏入Q3新增搜索预算：冻结种子继承成本与新增Q3成本分列。主比较含固定计划硬件消融与额外搜索调度消融，额外搜索相对未搜索基线不冒称等预算算法优势。算法确定性，重复运行核验计划及完整官方输出。

### 预处理合同、验证与敏感性

复用q1_io.prepare的无损字节导入、validate_graph及manifest；所有正式输入仅来自数据/processed/q1。100图、固定C=1048576、bL=250和bD=60用于主实验。开发代表图预定case001、005、019，扩展分析可按全量审计选出退步或命中边界案例，须标明事后诊断。容量扫描0、256KiB、512KiB、1MiB、2MiB；带宽扫描60、125、250、500，均固定Q2计划，非官方主成绩。

检查项：FIFO事件逐条重放及命中字节守恒；使用实际全局起止时间重建L1/UB物理实例峰值；跨核释放；COPY总量恒等式；无L2与Q2冻结指标一致；C=0与B的周期及搬运一致；最终选择不劣于L2种子；最终独立官方重放逐字段一致。若发现不一致，保留失败记录、定位实现或口径并修复后重跑，绝不修改原始官方程序。不得宣称全局最优、硬件实测或正式门禁通过。

### 文献与参考工程的适用边界

本轮核读本地SIGMA论文（Hoffmann等，arXiv:2606.03519v1，2026，摘要与§2）及T10论文（Liu等，SOSP 2024，DOI:10.1145/3694715.3695955，摘要与§1）。SIGMA支持“负载、通信、内存需共同考虑”的设计思路，但其分布式GNN划分目标没有本题FIFO发射/完成状态，不能直接作为Q3缓存模型。T10研究核间互连及分布式片上存储的计算通信权衡，可支持机制讨论；本题只读共享L2不能替换成其核间scratchpad直接访问。本文不借用两篇论文的实验数值，也不声称实现了SIGMA或T10。

参考工程Q3保留Q2计划的做法用于硬件消融；本研究另记录重新选优调度。结果包是参考工程，不是题面或官方算法规则。缓存规则以本题附录D.5/D.6及未修改problem_3源程序为准。

2026-09-24敏感性反例已复算：case005五核固定Q2计划，在L2带宽60、125、250、500下分别为48200、48202、48259、48253周期。60与250下命中次数为242与236、命中字节379308与374316。证据位于q3-sensitivity-r01/bandwidth_counterexample；该数据仅说明改变带宽会内生改变事件及命中序列，不能将其表述为“降低带宽一般能加速”。主实验始终用250。

求解实现补充：r02邻域以惰性队列枚举替代Q2的全量候选构造；保留前32组优先级、按核和合法插入位置的顺序以及按组轮转规则。开发集3图×3核数×普通/引导共18组、每组至多12候选逐字段一致。此优化不改变数学模型、评分器或邻域顺序；全量固定新增4个候选机会，种子和最终重放另外记录。

### 题面“共享输入”与实际查询范围的复核

题面1.5与问题3说明L2“用于复用多核共享输入”，但附录D.5明确在COPY_IN发射时按逻辑tensor id查询，并未排除spill reload或跨核中间张量。官方`cache_eligible`也只判断op_type是否COPY_IN。因此采用“所有可缓存COPY_IN按逻辑id查询”的详细规则，而不额外添加只准原始图输入的限制。DAG张量为单次定义的数据，同一逻辑id跨物理实例重用不表示写回式L2；COPY_OUT不查询也不主动填充Cache。

事后机制核验选取case044与046的高单核命中案例：case044单核216次命中、4408416命中字节全部属于spill_reload；原始输入61次均未命中。case046单核144次命中也全部为reload。case005五核的命中字节中190764来自原始输入，183552来自跨核输入。本段是对官方时间线与物理实例的分类重算，不把分类代理当成新的评分规则。

case044单核容量0、64KiB、128KiB、256KiB、512KiB、1MiB、2MiB下周期分别154407、154272、154407、154407、134726、107814、107814。容量敏感性具有阈值和小幅非单调，不拟合未经验证的普遍函数。

预算检查：预定3图×核数1/3/5共9组，将候选上限4增为12，9组采用周期均未进一步下降；保留4作为本轮高效配置。此为开发样本的预算检查，不是全量邻域收敛证明，完整候选序列仍可通过CLI的--budget扩展。稳定性以确定性重复和跨平台同输入输出验证，不伪造随机种子统计。

### 固定配置负收益的关键路径复核

500组固定方案硬件对照发现6组周期变差，不能写成“所有案例均受益”。最大例case067双核从32040130升为32217720周期，增加177590周期（约0.5543%）。以同一展开图、相同Pipe偏序和实际观察操作时长重构两个执行的关键路径，所有开始时刻残差为0、路径长度等于官方Makespan；两条路径中的Cube/Vector计算时间分别同为29532512与1030688周期，差异集中在关键路径搬运段：无L2为1476930周期，L2为1654520周期（其中Cache读39232周期）。

这是观察到的关键路径分解，不是将每个搬运时长改变量相加作为反事实节省。L2分流会内生改变并发集合、后续发射时序和关键路径上的DDR等待，不能用“总DDR字节减少”推导Makespan必降。本题固定模拟的此例必须如实保留，不修改评估器来强制单调。


### Q3 保留基线后的定向探索（2026-09-24，explore-r01）

继承上述共享FIFO离散事件模型，不改变硬件配置、官方Task构造或评价器。原500组full-r02的3005个文件通过SHA256清单冻结；新方法从其最终选中方案出发，不覆盖基线。本轮仅探索求解算法：①依据原运行COPY_IN的DDR占用时长筛选子图，把同一张量其他消费者的实际读取完成时刻作为同核合法插入位置的目标；②按共享输入字节量排序子图对，尝试向关联消费者所在核心迁移；③按计算负载选取大子图，沿原图拓扑序二分，尝试将后半部分迁移至负载较低核心。所有时间和亲和度仅生成候选，不视为真实节省量；每个候选必须由官方L2评估器重算。

三类方法与普通邻域各自从同一冻结方案独立搜索，每类最多8个不同合法候选，严格按(Makespan,逻辑额外搬运字节)保留最优；最终完整回放、FIFO账本及全局内存审计。没有足够合法候选时如实报告实际调用数，不填充重复调用。发现集8组为(1,3),(5,5),(19,3),(44,1),(46,1),(67,2),(84,2),(97,4)；验证集4组为(12,5),(33,3),(72,4),(92,3)，括号分别为图号与核数。验证集是针对新增算法预留的案例，不宣称从未观察过该数据集。

全量扩展条件在运行前固化：同一新方法须在发现集至少3组严格改善、耗时比均值不低于1.005，并在验证集至少2组改善、耗时比均值不低于1.002，同时全部正确性核验通过。不能在查看结果后通过组合各方法赢家来规避此条件。未达标则只保留诊断成果，不启动新的500组全量实验。


### Q3 本地论文研读补充

已筛读paper目录全部10篇，重点核读T10、DAN-Scheduler、CNGA、SIGMA的方法章节。逐篇页码、方法迁移条件和不可采用的硬件假设见[研读记录](审查/问题三论文研读与优化依据_r01.md)。拟进一步验证关键路径、FIFO驻留窗口与组迁移联合引导，并比较容量/带宽改变时固定方案与重新优化方案的差异。本条仅登记待验证算法建议，不改变已有模型身份、实验成绩或正式阶段状态。


### Q3 关键路径与FIFO窗口搜索 r02（预先登记）

本轮继承共享FIFO离散事件模型。以原full-r02方案保底，使用官方实际操作时长重构关键链并要求全部开始时刻重构残差为零；其持续时间是观测解释量，不是反事实可节省量。从有序insert/evict事件生成缓存驻留区间，再按关键链上的未命中读取优先生成合法同核插入候选。联合邻域增加共享同一张量的最多3个子图一起迁移，候选仍需完整重算，不直接提交开始时刻或改变FIFO。

方法为control（原事件调整8+拆分8）、window（窗口候选最多16）、joint（窗口最多8+联合迁移至总共最多16）。发现集(5,5),(12,5),(67,2),(84,2),(92,3),(72,4)，新增算法验证集(7,5),(23,3),(38,4),(61,2),(76,5),(99,4)。这些图已有基线成绩，验证集仅指未参与上一轮新增算法试验，不宣称数据从未观察过。全量扩展条件预先固定：新方法发现集至少3组改善、平均耗时比≥1.005，验证集至少3组改善、平均比≥1.002且高于control，全部核验通过，否则不扩展。

另设两图五核的18组容量×带宽诊断：C={0.5,1,2}MiB，B={125,250,500}bytes/cycle，各配置以同一冻结方案作为固定方案，joint在相同8候选上限下重新优化。用于研究调度随硬件参数的适应，不纳入正式500组固定配置成绩，也不作为未见案例泛化证据。局部Pareto候选池及迭代重诊断尚未在本轮实现，不声称完整复现T10或DAN。


### Q3 瓶颈分类与迭代重诊断 r03

沿用原模型和full-r02基线。按官方观测关键链的compute/DDR/CACHE_READ/同步延迟周期占比，将最大占比类别作为候选分配标签；该标签是已执行方案的描述性主导成分，并非因果瓶颈证明。compute优先拆分，DDR优先共享张量组调整，cache优先FIFO窗口，sync优先关键链普通迁移；其余邻域仍按权重穿插，避免仅搜索一种候选。

比较single（分类后单轮）、iterative（分类后迭代）、unclassified_iterative（不分类均衡穿插但迭代）三种算法。总上限24个不同候选；迭代每4个候选一批，有严格字典序改善则以该批最好方案重新诊断并生成新邻域，无改善则继续当前候选，耗尽或无新候选终止。每轮诊断的观测关键链周期和必须等于当前Makespan，接受后完整重放的保底目标不增加。旧FIFO窗口不得跨已接受方案直接复用。

发现集保留(5,5),(12,5),(67,2),(84,2),(92,3),(72,4)；新增算法验证集固定(9,5),(27,3),(41,4),(58,2),(81,5),(95,4)，均未用于前两轮新增算法试验，但已有原始基线，不称为从未观察的数据。全量扩展条件在服务器运行前固化：iterative发现集至少3组改善且平均基线/新周期≥1.005；验证集至少3组改善且均值≥1.002，均值严格超过两类对照，并至少2组严格优于single；全部核验通过。否则不扩展。保留既有容量/带宽诊断，单独冻结哈希，不覆盖或重算其数值。


### Q3 ASAP 式关键等待压缩试验 r04（2026-09-24）
继承问题三共享 L2 仿真模型，仅增加求解邻域，不改变核内评估器或缓存规则。由固定运行的精确关键图提取队首阻塞，在原核内前移受阻的独立子图或释放依赖的生产子图。合法插入区间同时满足原图依赖与其他核顺序；观察到的空闲时间只用于候选排序，不能解释为确定可节省周期。以官方重放的 (总周期, 额外搬运字节) 字典序接受方案，并校验 FIFO、同步、内存峰值。
对照为 r03 无分类迭代，另设 ASAP 固定根邻域与保留根分支的 ASAP 搜索。每项最多 24 个唯一候选，分支轮转每批 4 次；邻域提前耗尽时如实报告调用数，不填充伪候选。发现组 case5/5核、12/5核、58/2核、67/2核；新算法验证组 case16/5核、31/3核、49/4核、64/2核、87/5核、98/4核。均以冻结的500组方案为起点，因此新验证指未用于本轮算法设计，而非未见过的原始数据。
扩量要求：保留根分支算法在验证组至少3/6缩短周期，平均降幅至少0.2%，相对两个对照分别至少2例更优且平均降幅更优，所有审计通过。另报告历史最好结果，防止重复发现旧收益。即使达标也先记录小样本证据；本轮不自动扩量，不提交交接材料。原500组及容量、带宽分析不变。正式阶段门禁 NOT_RUN。


### Q3 预算保留与缓存-ASAP组合试验 r05
继承Q3模型与官方评分，不引入新物理模型。比较旧beam、仅ASAP的reserved、ASAP/cache固定轮转fixed及收益分配adaptive四方法，均至多24个唯一候选。新方法先在原方案上评估至多16次；组合方法轮转两个候选族，之后原方案/当前最好交替分配。adaptive按候选相对父方案的正周期收益均值选族，每第4次轮转探索。收益是观察值，不作因果或最优性宣称。邻域耗尽释放预算，不能保证原方案24候选全被覆盖。
发现组49/4核、58/2核、67/2核、98/4核；新算法验证组21/5核、36/3核、53/4核、89/2核。扩量条件：adaptive在验证组至少2/4比基线更优，平均降幅至少0.2%，相对beam至少2/4更优且平均降幅严格超过beam/reserved/fixed，所有审计通过。单次小样本不自动触发500组扩量。保持基线3005文件与参数198文件不变，不提交或派稿。保存真实评估次数与作业用时；同预算不等于同耗时。ASAP惰性候选使用同序生成与合法区间缓存，case12与原版11个候选完整一致。正式门禁NOT_RUN。


### Q3 关键再读与淘汰来源干预 r07
按用户指定比较消费者前移、直接触发淘汰的插入来源后移、二者联合三种合法同核子图次序干预。先只读扫描冻结500组中的100个五核方案，使用官方任务结构和观测时长精确重建一条关键链；用真实有序FIFO事件关联fill→eviction→critical miss，发生重新插入时清除旧淘汰证据，禁止把旧生命周期原因沿用到新生命周期。
筛选依据为存在明确事件证据及每个干预族至少一个合法候选；按关键再读观测时长/总周期降序、案例号打破平局。已参与r01-r05算法设计的案例池选前4个作为发现组，其他池选前4个作为新验证组。该验证为机制条件下的验证，不代表全100图随机样本；原始图均已参与基线。筛选不足4+4时停止，不悄悄更换条件。
每种方法同一起点、固定邻域最多24个唯一候选，最多取32个关键事件，按事件交错枚举。单步位置取合法最近、靠近事件时间的启发位置与合法极端，联合尝试两种次序并确认最终消费者位置前移、来源位置后移。原图依赖与其他核顺序无环约束必须满足；分组和核归属不变。位置时刻只作候选提示，重排后真实FIFO与带宽完全重算。固定起点使三臂消融清楚，不在本轮同时引入重诊断迭代。
每个候选由官方默认参数评估，以总周期及额外搬运字节字典序筛选，保存目标消费者组对应张量的全部命中/未命中事件（不能认为生成op id跨方案稳定）。终选重复重放、FIFO和真实内存审计。通过新验证的条件：joint至少3/4优于冻结起点，平均周期降幅≥0.2%，分别至少2例优于两种单步并且平均降幅更大；必须完成审计并核对历史最好。未通过不扩量。原3005份基线与198份参数分析文件不变，不提交或派稿，正式门禁NOT_RUN。


r07运行前修订：100例筛选完成，仅12例满足三臂可行性，其中既往发现池只有3例（67、72、76，均5核）；未参与既往优化设计池按既定排序选择73、14、3、66（均5核）。原筛选程序按不足4+4的检查以AssertionError停止，未评估任何新候选。保留原selection.json及失败状态，另存selection-amended.json，仅将发现组数量改为3，验证组4例及其收益门槛不变。之后执行3+4例×3臂共21项。此修订发生在所有优化收益产生前，不放宽事件证据或合法性条件。

### Q3 状态转移、服务负载与收益界补强 r08

本节补全上述离散事件模型，不另立模型、不改变算法、缓存规则或原500组数值。参数记为缓存容量C、缓存带宽b_L、DDR带宽b_D，单位分别为字节及字节/周期。每个方案p包含切图、核归属和每核子图顺序；四Pipe发射顺序、内存依赖和开始时刻由官方展开与评价确定，不属于可自由指定的决策。所有结论均先说明是固定方案、固定执行、松弛界还是全局最优界。

#### 1. 完整的事件状态和FIFO转移

用k标记官方确定性次序中的微事件；同一整数时刻可有多个微事件，不用含糊的“时刻前状态”合并它们。定义

$$
Z_k=(Q_k,u_k,\sigma_k,\boldsymbol c_k,\boldsymbol r_k,E_k,\boldsymbol w_k),
\qquad Q_k=(q_1,\ldots,q_m),\quad u_k=\sum_{j=1}^m s_{q_j}.
$$

其中Q为逻辑张量FIFO队列，σ为操作pending/running/done状态，c为各核Pipe队首游标，r为未满足前驱数和外部释放时刻，E为各Pipe在飞操作及预测完成时刻，w为DDR/L2服务池中的剩余归一化工作量。给定p和参数，从空缓存及全pending状态，按事件算子Z_{k+1}=F_{e_k}(Z_k;p,C,b_L,b_D)演化。

正字节COPY_IN i发射时，取其逻辑张量ℓ_i，定义

$$
h_i=\mathbf1\{\ell_i\in Q_k\},\quad
\rho_i=\begin{cases}L,&h_i=1,\\D,&h_i=0.\end{cases}
$$

查询本身保持Q与u不变；h_i一旦确定，在该次读取飞行期间不改变。COPY_OUT不查询、不填充。COPY_IN完成时，对逻辑键x执行：

$$
(Q_{k+1},u_{k+1})=
\begin{cases}
(Q_k,u_k),&x\text{无有效逻辑键，或 }s_x>C,\text{ 或 }x\in Q_k,\\
((q_{j^*+1},\ldots,q_m,x),\ u_k-\sum_{r=1}^{j^*}s_{q_r}+s_x),&\text{其余情形},
\end{cases}
$$
$$
j^*=\min\left\{j\in\{0,\ldots,m\}:u_k-\sum_{r=1}^{j}s_{q_r}+s_x\le C\right\}.
$$

有效零字节张量的完成插入仍按官方代码处理，但不计入命中率；本节数值负载公式只统计正字节搬运。完成插入并不以“发射时未命中”为前提：命中读取在飞时若被逐出，完成时可以重新入队。同键的并发冷读各自使用DDR，不合并；先完成者插入，后完成者若发现已驻留则不刷新。

事件优先次序继承官方：先结算服务工作并退休到期操作、依次处理完成填充，再处理外部释放，再按核编号和PIPES顺序尝试发射；同刻完成项遵循官方executor遍历次序。跨核目标读取至少在对应COPY_OUT完成后500周期释放；发射还需前驱满足、当前Pipe队首及空闲槽位。不得把同刻填充与查询任意交换。

以下不变量由空初态和上述转移归纳得到：①队列逻辑ID唯一；②u等于队列张量大小之和且0≤u≤C；③幸存项相对插入顺序不变，查询不刷新次序；④每次正字节查询恰为hit或miss之一；⑤每个搬运最多属于一个带宽池，每核每Pipe最多一个在飞操作；⑥操作只在前驱与释放条件满足后发射。容量不变量的归纳步骤来自j*的最小可行性；唯一性来自入队前排除已驻留键。缓存命中没有豁免任何调度约束。

#### 2. 双服务池：命中率到服务负载的一般关系

对官方展开的单张量COPY，独占服务工作量为q_i^D=ceil(s_i/b_D)、q_i^L=ceil(s_i/b_L)，正字节下至少1周期。一般实现以官方_op_duration取值为准。令A_r^+(t)为池r中剩余工作严格为正的操作集合，则

$$
w_i(a_i)=q_i^{\rho_i},\qquad
\dot w_i(t)=-\frac{1}{|A_r^+(t)|}\ (i\in A_r^+(t)),\qquad w_i(t)\ge0.
$$

某项降至0后，即使等待整数退休时刻，也不再占用正工作份额。到达与退休时先结算工作、再重新预测完成时刻，并按官方上取整规则和1e-9数值容差执行。不能把带宽池写成所有操作独占b_r，也不能合并两个池。其总服务率至多为1，故一次完整执行满足

$$
W_D=\sum_{i\in I}(1-h_i)q_i^D+\sum_{j\in O}q_j^D\le T,\qquad
W_L=\sum_{i\in I}h_iq_i^L\le T.
$$

I为合资格正字节读取，O为其余进入DDR池的搬运；若零字节COPY仍按官方规则入池，其q_j按官方至少1周期计入W_D，字节负载则贡献0。本项目核验的轨迹没有ON_CHIP COPY；若推广到存在该路径的数据，必须另计该路径，不能直接使用下式。记读取字节R=Σ_I s_i，其余DDR字节X=Σ_O s_j，命中字节H=Σ_I h_i s_i，字节命中率H_B=H/R，则

$$
D_D=X+(1-H_B)R,\quad D_L=H_BR,\qquad
L_{\rm bytes}(H_B)=\max\left\{\frac{X+(1-H_B)R}{b_D},\frac{H_BR}{b_L}\right\}\le T.
$$

该式是普遍适用于此搬运口径的服务负载下界，不是总周期等式。整数工作量界W_D、W_L更强；每个正字节搬运的取整误差小于1独占周期。增加命中减少DDR需求，同时增加L2需求；即便R、X固定，二者最大值也只在分流均衡点之前下降。均衡命中字节为z_bal=(R+X)b_L/(b_D+b_L)，但它未必可由FIFO实现，也可能大于全部读取字节。

每核每Pipe串行还给出计算负载下界K(p)。将原始图中COPY权重放宽为0，保留所有非COPY的固定计算周期，记最长依赖路径为L_DAG^0，各Pipe的总计算为W_q^0，则任意n核合法方案都满足

$$
L_0(n)=\max\{L_{DAG}^0,\max_q W_q^0/n\}\le T.
$$

证明：原始计算节点不能删除或复制成更快节点；任一原图依赖路径必须顺序完成，每种Pipe最多由n核并行提供服务。程序另核对每个原始计算节点的周期在官方轨迹中完整保留。于是固定执行的可核验界为

$$
L_{\rm trace}=\max\{L_0,K(p),W_D,W_L\}\le T.
$$

同时，对官方展开后的数据/内存依赖、Pipe前后继与跨核500周期边组成的图G_p，以实际操作持续时间d_i=f_i-a_i为权重，有

$$
a_i=\max_{j\to i}(a_j+d_j+\ell_{ji}),\qquad
T=\max_{\gamma\subset G_p}\left(\sum_{i\in\gamma}d_i+\sum_{(j,i)\in\gamma}\ell_{ji}\right).
$$

此式用于观测执行的精确重构；本轮6次参数重放均零残差。d_i内含竞争并随整个事件过程变化，不能把当前关键路径上所有“DDR与L2独占时长差”相加当作可兑现收益。由此得到完整关系：h由状态转移内生决定→改变两个池的工作量与并发集合→改变操作时长、释放和关键路径→决定T。仅用一个H_B不存在普适的确定性S=f(H_B)。

#### 3. 容量、带宽参数下的乐观松弛与收益界

固定p时，官方展开与COPY多重集合不随C、b_L变化（DDR带宽、L1/UB配置保持固定）。对逻辑张量x，令读取次数m_x、大小s_x。空缓存下至少第一次查询未命中；s_x>C则全部旁路。因此任何该方案执行的命中字节满足

$$
0\le H\le H_{\max}(p,C)=\sum_{x:s_x\le C}(m_x-1)s_x.
$$

H_max不是可达命中字节：它忽略同时驻留总容量、FIFO替换与并发冷读，只保留“单张量能装入”和“首次必冷”两个必要条件。对命中字节z作连续分流松弛，得到一维凸问题及闭式解：

$$
\Phi(p;C,b_L)=\min_{0\le z\le H_{\max}}\max\left\{\frac{R+X-z}{b_D},\frac{z}{b_L}\right\},
\quad z^*=\min\left\{H_{\max},\frac{(R+X)b_L}{b_D+b_L}\right\}.
$$
$$
L_p(C,b_L)=\max\{L_0(n),K(p),\Phi(p;C,b_L)\}\le T(p;C,b_L).
$$

证明：实际H在松弛区间内，实际两个池服务至少需要字节数/带宽；对更大的松弛集合取最小值只会降低下界。K与L_0独立成立，取最大仍是下界。上述界在本轮100个五核结果、72份既有参数结果和6次重放中全部通过核验。脚本同时保存采用实际H的较弱字节负载界；6次重放另计算整数W_D、W_L。

固定p时，C增加使H_max不减、松弛可行区间扩大；b_L增加使第二项逐点不增。因此L_p关于C和b_L均不增，可出现平台。它描述乐观性能边界，而非官方FIFO曲线必须单调的定理。100个五核结果的T/L_p范围约1.0039～2.9829，说明部分案例界较紧、部分很松，不能把界的剩余差距全部解释为可优化空间。

边界行为也可直接推导：C=0时H_max=0，Φ=(R+X)/b_D；当C至少达到该方案最大单张量大小时，本松弛的H_max饱和为R−Σ_x s_x。再令b_L趋于无穷，则

$$
\lim_{C\to\infty,\ b_L\to\infty}L_p(C,b_L)
=\max\left\{L_0,K(p),\frac{X+\sum_x s_x}{b_D}\right\}.
$$

因此即使乐观地消除所有可复用读取的L2服务时间，首次读取、写出和计算仍构成不可消除的负载。松弛在“最大单张量大小”即饱和不代表真实FIFO在该容量已饱和：真实执行还要同时容纳多个张量。这一区别解释了为什么不能用本松弛的容量阈值代替硬件选型结论。

记实际合法方案全集为P_B、已官方评价方案集为A，定义

$$
T^*(C,b_L)=\min_{p\in P_B}T(p;C,b_L),\quad
U_A(C,b_L)=\min_{p\in A}T(p;C,b_L),\quad
L^*(C,b_L)=\inf_{p\in P_B}L_p(C,b_L).
$$
$$
L_0\le L^*\le T^*\le U_A.
$$

对参数无关的正参考耗时T_ref，最大加速比及最大时间收益满足

$$
\frac{T_{ref}}{U_A}\le S^*=\frac{T_{ref}}{T^*}\le\frac{T_{ref}}{L^*}\le\frac{T_{ref}}{L_0},
\qquad
1-\frac{U_A}{T_{ref}}\le G^*=1-\frac{T^*}{T_{ref}}\le1-\frac{L^*}{T_{ref}}\le1-\frac{L_0}{T_{ref}}.
$$

本轮已经数值计算L_0和各已评价p的L_p，没有求解整个P_B上的L*或T*。不得把min_{p∈A}L_p当作全局下界，也不得把U_A改名为“最大可达收益”；对有限候选集合A内部，则min_A L_p确实是min_A T的下界。容量/带宽改变时各次搜索生成的A可能不同，比较U_A时需披露该差异。固定方案的T_ref/L_p仅是该方案在相应参数下加速比的上界，不约束其他切图与调度的改进幅度。

全局最优函数至此有了明确的数学定义与可计算的保守包络，但全局紧界和最优性证书仍未取得。这一限制不能靠拟合容量/带宽曲线消除。

#### 4. 非单调的两类机制与官方事件证据

容量不能简单解释为“原缓存加一些空间”：不同容量下可插入的张量集合、淘汰顺序及驻留集合可能不同，尤其变长张量会跨越旁路阈值。第44图同一单核Q2方案，C从65536增至131072时：

- 大小73728的逻辑张量1000000053，在小容量下始终旁路；在大容量下可以插入。
- 两次执行中，张量1000000071均在16034完成填充。大容量执行在25214由1000000053的插入将其淘汰，小容量无该插入。
- 同一操作1000001653在30486查询：小容量命中，大容量未命中。完整轨迹共11个读取命中决定发生改变，命中次数11→0、命中字节26688→0。
- 总周期154272→154407，增加135。两条观测关键链的DDR项80035→80278、Cache项36→0、M项66102→66064、V项8099→8065，差额243−36−38−34=135。

这是变长张量准入变化引起的FIFO污染证据，不把该特定案例笼统归因为经典等长页面反例。一般FIFO不具备容量嵌套保证的背景可参见Belady、Nelson、Shedler（1969），IBM作者机构页面：https://research.ibm.com/publications/an-anomaly-in-space-time-characteristics-of-certain-programs-running-in-a-paging-machine 。此处本题结论来自官方逐事件重放，不借用文献实验数据。

带宽增大即使缩短单次命中的独占工作量，也会改变完成与发射次序，进而改变冷读重叠和命中集合。第5图同一五核Q2方案，b_L由60增至250：

- 忽略纯时间变化，缓存事件序列在索引56首先出现次序差异，14个相同(core,op)读取的命中决定改变。
- 核0操作1000005045读取张量1000000740：低带宽时先于3741填充、再于3820命中；高带宽时该读取在3661提前发射，当时尚未填充，因此未命中。另有读取反向由miss变hit，不能只选不利事件。
- 命中次数242→236，命中字节379308→374316；DDR整数服务工作28262→28349，L2工作6477→1630。尽管L2工作下降，总周期48200→48259。
- 两条关键链DDR项7920→7890，Cache项170→29，M项17274→17448，V项8836→8892，同步项均14000；总差额−30−141+174+56=59。

最后一个分解说明关键路径本身发生切换，不能将服务工作减少直接当作总周期减少。上述差额是两条观测关键链的恒等分解，不宣称每个变化项分别具有独立因果效应。

只要事件排序、命中集合及活跃集合不变，就可在该事件区域内分析带宽对工作量的作用；跨越填充/查询先后、容量准入或FIFO淘汰边界时，必须重算事件过程。即使区域内也不能省略整数取整与关键路径切换。松弛包络单调与官方实测曲线非单调可以同时成立；当前反例只证明固定方案非单调，没有证明全局T*非单调。官方强制FIFO与ASAP不允许自由禁用命中或任意等待，因此也不能以“可模仿较差硬件”为由未经证明断言T*单调。

#### 5. 命中率双维度与收益汇总口径

同时报告H_N=N_hit/N_read与H_B=B_hit/B_read；正字节COPY_IN作为共同分母范围，零分母统计定义为0并说明无有效读取。对J个图，宏平均为J^{-1}ΣH_j；微平均为Σ分子/Σ分母，分别按次数或字节加权。官方cache_stats.hit_rate是字节命中率，不能标成次数命中率。

原500组中100个五核最终方案的本轮核验结果：

|命中率口径|逐图等权平均|汇总后加权比例|
|---|---:|---:|
|次数命中率|21.7936%|29.5146%|
|字节命中率|26.5921%|35.9182%|

这四个数字都不代表关键路径上的可节省时间；禁止从35.9182%直接推出类似比例的提速。五点平均加速比之比与逐图同核时间比的平均也不同：五核Q3/Q2平均加速比之比提升1.9327%，逐图T_Q2/T_Q3平均为1.022567（提升2.2567%）；两者均真实，回答的汇总问题不同，必须注明。

#### 6. 本轮证据范围和复核结果

证据：图表/runs/20260924-A-q3-theory-r08-v3/evidence.json及6份官方重放结果；程序：程序/q3_theory_audit.py。100个五核冻结轨迹、36组既有参数配置的fixed/selected共72份轨迹、6次参数重放，合计178份FIFO账本与负载界通过；6次重放关键路径零残差，4份带宽结果与既有JSON全部字段一致。原3005份基线、198份参数文件及官方输入/代码哈希核验未变。

开发核验第一次因组合下界遗漏L_0而停止，第二次因Python整数键与JSON字符串键直接比较而停止；保留两个输出目录，不计作成功实验。修正为max(L_0,K,Φ)及统一JSON表示后，以新目录完成最终核验。该过程未修改官方评分器或旧结果。本轮是理论与轨迹核验，不是新算法扩量，不产生新的500组成绩，不提交或派稿，正式门禁仍NOT_RUN。
