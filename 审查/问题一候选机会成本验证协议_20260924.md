# 新增候选机会成本验证

机器协议见同名JSON。冻结单核、12评价机会、四粒度保护、seed0、2～5核。仅对新增memory/hybrid路由做严格正局部预测收益筛选；当前最优方案核内时间复用已取得的官方step3记录。比较双方使用相同局部任务依赖回放，不与不同口径全局时间直接比，不新跑隐藏全局评分。跳过时保持原随机数消费、评价机会、观察状态与后续搜索。

先运行八图32配置guarded版本，对照上一轮同预算证据；仅当两类目标图各自总时间不增加且8个大图保护配置无退步时，扩展16图64配置，同时运行unguarded与guarded共128次。扩展样本按两类结构内算子数等距排名预选，不根据新结果选择，不宣称这些图从未参与历史研究。

精确收缩加速对两组均启用，减少求解开销不改变候选语义。对完全相同方案，可复用已有原版最终复核，但必须核验官方输入包、配置、原图、方案字节、完整结果相等和原版检查文件哈希；不复用转手记录。新增方案仍原版复核。每次完整搜索评分照常计预算，不把复核复用当求解额外机会。

若扩展出现退步，保留逐例和拒绝台账，不按case白名单回改规则，不据此自动替换默认或声称新的100图成绩。
