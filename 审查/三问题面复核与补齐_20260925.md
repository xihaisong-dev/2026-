# 三问按题面重新审核与修正（2026-09-25）

结论：定版模型的场景划分、主次目标和官方评价口径与题面一致；最新成果尚不能标记为完整提交通过。当前缺口主要是覆盖、交付接口、计时和论文版本一致性，并未据此修改定版搜索算法。

题面来源：题目/赛题原件/通用神经网络处理器下的多核调度问题.docx。逐段正文、OMML文字与脚注重提取见审查/证据/20260925-A-q123-requirements/，来源SHA见source.json。段号按此次提取，不是原文件页码。

|要求|题面依据|检查结论与处理|
|---|---|---|
|所有非COPY操作恰好覆盖、子图DAG、各核顺序合法|[25][28][103–106]|规范导出700份当前方案；全部调用原版validate_multicore_plan。格式检查不冒称重新做了完整事件模拟；原完整评价沿用已验证证据。|
|Q1跨子图均DDR，同核Task清空|[39][142]|定版遵循场景A官方评估；未引入同核跨Task免费复用。|
|Q2每核所有子图合成一个Task|[53][143]|protected使用官方场景B；同核数据生命周期和跨核COPY_OUT完成后500周期释放不变。|
|Q3共享只读FIFO，DDR/L2独立带宽|[44][59][149–150]|combined官方模拟+FIFO审计；不以命中率替代Makespan，不将物理DDR流量与官方额外COPY混用。|
|Q1/Q2的1～5核平均加速比|[48][55]及脚注|Q1完整。最新Q2缺1～4核，已启动补齐；不得拼接旧组合成绩。|
|Q3同核无L2/只读Cache两配置曲线与比值|[61]|已启动最终同方案两种配置评价。不同方案Q2/Q3周期比另有含义，不当作纯硬件收益。|
|逐例Makespan/搬运/命中率|[49][56][62]|五核已有周期与搬运；Q3补同方案无L2和命中率列，自动报表只在覆盖完整后生成完整表。无L2命中率不伪造为缓存效果。|
|固定配置和官方程序|[82][101]|读取官方manifest逐文件校验，不修改L1/UB、DDR、同步延迟或L2参数。|
|可复现Python与规定方案文件名|[50][57][103]|新增q23_submit_current.py，显式验证暖初解、核心数和格式，输出case名规范JSON；旧final.plan.json保留内部证据用途。|
|单用例5～10分钟为佳、避免暴力搜索|脚注[2]|定版为有界启发式。590秒含求解与最终重放，不含历史初解；不宣称冷启动达标。补充无L2评价单独计时，不偷算进搜索指标。|
|平均加速比口径|脚注[3–4]|固定整图单核参考，逐图比值算术平均；不使用总周期比替代。|

## 已修复

- 新增程序/q123_delivery_export.py；输出图表/runs/20260925-A-q123-requirements-export，Q1 500份、Q2/Q3各100份标准命名方案，export_manifest记录来源与输出哈希，未改变方案语义。
- 新增程序/q23_submit_current.py，采用已定版完整组合而非旧默认提交入口；暖初解必须显式提供，核数不匹配提前报错，最终官方复核通过后才导出规范文件。默认590秒、384提议、种子0。case001两核Q2/Q3短流程冒烟均通过；这不是全量性能测试。
- 新增程序/q23_delivery_complete.py，独立输出目录与原始评价；900项补齐任务不覆盖现有结果。单核与Q3同方案无L2三项本地实际图测试均通过。
- 新增程序/q23_completion_report.py；只有两问各500个唯一有效配置齐全才生成完整逐例表/五点结果，缺项返回非零，不拼接旧低核数成绩。

## 服务器补齐计划与边界

目录/home/xihs/q1-campaigns/20260925-q23-requirements-completion，15并发。600项为Q2/Q3各100图×2～4核新求解，200项为单核核验，100项为现有Q3五核最终方案无L2补评。Q3低核数求解另附同方案无L2评价。搜索仍590秒，额外附录评价时间单列；外层2400秒仅用于杀停异常补评/基准任务，不扩大搜索预算。

Q3低核初解使用r07已验证Q2方案和r02锚点，五核保留既有冻结初解集；必须披露这种暖初解来源差异，不能写成统一冷启动过程。求解组合本身不变。新低核求解失败时不会把旧低核结果冒充新结果。

## 论文材料不足（未宣称已修复完毕）

现有论文/章节/0_abstract.tex与9_evaluation.tex仍给出旧Q2=4.159974、Q3≈4.240375，且称三问均有500组；作为旧版历史稿可以成立，作为最新定版稿不成立。当前PDF/DOCX未按新组合重建，不能直接提交。已在论文README和总说明标注版本警示；不只换摘要几个数字而保留旧算法章节、图表和附录。

补齐数据后必须联动更新摘要、Q2/Q3求解章节、1～5核图、同方案L2比值、逐图附录、核心代码附录及结论，再编译逐页核验。封面身份字段仍为空，AI声明开关与正式状态仍需按已有流程确认；本轮没有代填身份、披露或人工验收。

正式门禁NOT_RUN，submit_ready=false。本轮只是题面、建模、代码和已有证据审核及补齐执行，不冒称论文版式和最终合规验收通过。
