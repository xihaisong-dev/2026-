# 三问全量补齐与冷启动核验（进行中）

## 范围与分工

本轮保持定版算法不变：Q1 routes_gate_reuse；Q2 protected＋GenerationReuse；Q3 combined＋GenerationReuse。补齐900项：Q2/Q3的1～4核各100图共800项，以及最新Q3五核最终方案的100项同方案无L2重放。结合既有Q1 500组和Q2/Q3五核200组，最终形成1500份方案和Q3 500组同方案双配置证据。

`图表/runs/20260925-A-q23-requirements-completion/allocation.json` 固定互斥分片。本机120项：80项小图单核复核及40项小图四核搜索，1工作进程；服务器780项（164项原先已启动，616项续跑），15工作进程。服务器另有2个冷启动压力工作进程。这里是主机进程数，与题目NPU核数不同。

旧服务器协调进程13296暂停派发，已启动子任务继续到各自row.json落盘，再终止旧协调进程并启动分片续跑。15项可能仅有worker记录，恢复行明确注明coordinator_exitcode_unavailable；未编造已丢失的父进程退出码与wall。完整方案和评价仍按哈希及官方字段复核。无重跑已完成项，无重叠分配。

## 真实命令

服务器：
```
/home/xihs/q1-runtime/run-python -u 程序/q23_completion_shard.py 图表/runs/20260925-A-q23-requirements-completion --owner server --workers 15 --drain-parent 13296
/home/xihs/q1-runtime/run-python -u 程序/q123_cold_audit.py 图表/runs/20260926-A-q123-cold-audit --workers 2
```
本机：
```
python -X utf8 -u 程序/q23_completion_shard.py 图表/runs/20260925-A-q23-requirements-completion --owner local --workers 1
python -X utf8 -u 程序/q23_collect_and_publish.py --paper-python C:/Users/Lenovo/AppData/Local/Programs/Python/Python314/python.exe
```
采集器通过交互密码连接，密码只在进程内存，不写入脚本或文件。分批下载服务器完成记录及最终方案/评价，ZIP传输做SHA256校验；本机任务不由服务器覆盖。无需重新启动同一个分片。

## 冷启动口径

服务器预先选定case001、014、048、067、072、076；本机分片完成后另选不重叠的case003、018、033、064、071的五核，从原始图依次执行Q1、Q2、Q3。Q2使用本次新生成的Q1初解，Q3使用本次新生成的Q2初解且不加载历史锚点。每阶段新进程、不复用求解器缓存；不清空系统页缓存。记录本阶段和累计前序阶段时间，最终复核包含在内。

这是真实图输入端到端压力核验，但只有11图5核，并非100图全核数耗时认证；初解与定版历史输入不同，因此不替换定版成绩。Q1额外2400秒监控、Q2/Q3外层650秒监控仅防进程失控，超出600秒会明确标记，不能称为满足十分钟。

## 材料与编译

`程序/q123_complete_materials.py` 只在900项完整且冷启动批次结束后执行：验证1000份Q2/Q3方案和评价哈希、Q3同方案搬运相等；汇入Q1后输出1500份标准文件，生成曲线、附录，重写当前算法说明/摘要/结论/结果章节并三次XeLaTeX编译。保留原稿备份。独立官方500组Q3的无L2列不再取另一份Q2方案冒充。

补齐之前已实测拒绝发布：命令退出1，提示Coverage incomplete，原论文正文未被新结果替换。算法新章节单独排版检查已编译成3页，抽看首页公式文字无裁切；这不是全文编译或全文视觉验收。

## 未完成和交付边界

实时进度以同目录combined-progress.json及各分片记录为准；不要把动态数字抄成完成结论。完整编译后的版面、引用、附录源码谱系及逐页视觉检查尚需执行。正式阶段门禁NOT_RUN，身份信息及AI披露尚未完成确认，submit_ready=false。不得把自动编译成功等同于可正式提交。

本机续接命令：`python -X utf8 -u 程序/q123_cold_audit.py 图表/runs/20260926-A-q123-cold-audit-local --cases 3 18 33 64 71 --workers 1`。时间核验中的前序失败会阻止后续问运行；批次complete表示核验尝试结束，不代表所有图通过600秒目标。
