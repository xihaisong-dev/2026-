# 问题一 main 发布核验

实际检出的提交：`278a6ad36e4f4360db1d9ee6b3a9809cee3319b3`；模型、代码及全量证据的产物提交：`d10e0ca16c38f5a14049f27cff09f0c275782320`。

从该提交用 `git archive` 导出程序与原始用户数据到仓库外的新目录，未使用工作目录的 processed 数据或缓存。

| 检查 | 结果 |
|---|---|
| 提交内 75 个 Python 源文件与 code_manifest 的 SHA-256 | 全部一致 |
| `python 程序/主程序.py prepare` | 退出码 0，审计导入 100 图 |
| `python -m unittest discover -s 程序/tests -q` | 退出码 0，70 项通过 |
| `python 程序/q1_submit.py 数据/processed/q1/data/case_001.json -n 5 --output output/smoke --verify-final` | 退出码 0，Makespan 50223 cycles，加速比 4.641498914839814，完整最终结果与未改动官方评价一致 |
| `python 程序/q1_writing_summary.py --output 图表/runs/20260923-A-q1-writing-handoff` | 退出码 0，重建 500 行与五点均值 |
| 冻结 CSV Git 对象与原始文件的字节一致性 | 通过；保留 CRLF，避免来源哈希因 Git 换行转换失效 |
| 当前发布的差异空白检查 | 通过（CRLF 按原样保存；冻结绘图 SVG 的生成器行末空白不改写） |

独立导出核验位置：`C:/Users/Lenovo/AppData/Local/Temp/q1-main-check-j10ob60g`。该位置仅用于记录本机实测，接收者应从 Git 重新导出到自己的新目录。

本次没有重新运行 100 图的全部搜索。此前全量官方输出一致性、6 个慢配置完整搜索与复现压缩包验证，见运行加速审查和交接单所列证据。本核验只确认已提交代码可独立读取原始附件、运行及复核，不替代接收者验收、非作者审阅或正式论文门禁。
