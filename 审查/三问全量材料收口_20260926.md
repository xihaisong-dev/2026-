# 三问全量材料收口检查（2026-09-26）

本记录是 A 端的结果和材料检查，不代替 B/C 接收、人工签署或正式阶段门禁。

## 数据与材料

- 本轮补齐任务 900/900 有效；三问标准方案分别 500 份，共 1500 份。
- Q3 最终方案另有 500 组同方案无 L2/L2 官方结果，避免以 Q2 的不同方案冒充硬件对照。
- 正文、平均加速比曲线、总周期曲线、缓存对照图和逐用例附录均已同步。
- 平均加速比采用逐图比值的算术平均；总周期比单列，不混为官方平均加速比。
- Q1 沿用先前已验证的 500 配置；本轮不是重新运行三问全部搜索。
- 算法采用版本没有改变。Q2/Q3 正式成绩仍基于显式历史初解，不能将其运行时间称为从原始图开始的总耗时。

## 编译和版面

- PDF：`论文/数模论文.pdf`，98 页。
- SHA-256：`07dca780e2f0d47fc19ba2757569bb41acd2502d1d94201f36e252a18e4ae79b`。
- 三轮 XeLaTeX 编译通过，编译警告清单为空。
- `BodyStart=2`、`BodyEnd=32`，正文共 31 页。总页数包含大量代码与逐用例附录，不能称正文 98 页。
- PDF 严格版面扫描：0 个关键问题；封面与末页为工具明确记录的两处例外。
- 当前有效 TeX 引用闭包的图表检查：0 个关键问题、0 个警告。原全目录扫描把未引用旧稿一起扫描，并按章节目录解析图片产生误报；保留原报告，另用项目脚本限定有效源文件并调用未修改的扫描工具。证据见 `审查/证据/20260926-active-paper/scope.json`。
- 已做全 98 页缩略图检查，重点放大查看 PDF 第 23、29、32、88 页；不是逐字人工审稿，也不代填 C 端验收。

复查命令：

```powershell
python 程序/q123_active_visual_audit.py
python 工具/pdf_layout_audit.py --workspace . --strict
```

完整数据验证、材料重建和编译命令（须有规范输入及原证据）：

```powershell
python 程序/q123_complete_materials.py --compile
```

该命令仍保留宽范围图表扫描结果，应再运行有效源文件检查，不应把宽范围退出码改为成功。

## 冷启动结论

11 张五核压力样本完成审计尝试，10 张完成 Q1→Q2→Q3 链。case014 的 Q1 达到 2400 秒监控上限后终止，后续两问没有执行。case072、case076 的 Q1 分别约 1728.1、1992.2 秒。case067、003、072、076 的三问累计链耗时约 1043.0、1009.0、2716.4、2973.0 秒。

此为 11 图样本，不是 100 图全核数冷启动认证；没有清空操作系统文件缓存。冷启动重新生成初解，与采用成绩的初解不同，不能用它替换正式成绩。现有证据明确不支持“所有配置端到端不超过 10 分钟”。

## 尚未满足项

1. 大图端到端耗时仍超出 5～10 分钟目标，须单独优化及复验，不能文字消除。
2. 用户此前提出正文 40 页以上，目前正文 31 页，尚未满足。后续应补充有依据的推导与验证，不能用附录、放大字号或无效文字补数。
3. 身份字段、AI 使用披露和正式流程门禁尚待完成；没有替用户签署或填写人工确认。
4. 旧 Word 文档未同步，不是本轮有效稿。当前有效交付是 PDF 和 LaTeX 源稿。
5. 当前交接可用于写作和审查，不能标记为已经满足全部正式提交条件。
