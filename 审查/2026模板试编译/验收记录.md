# 2026 模板试编译记录

日期：2026-09-21。范围：本地 XeLaTeX 模板与排版示例；检查者为本任务主代理及独立格式审阅代理，不代表实际队员签字验收，也不代表正式比赛论文通过 ASSURANCE。

| 检查 | 结果与证据 |
| --- | --- |
| 示例编译 | `python 协作/工具/compile_template.py`，XeLaTeX 三遍，退出码 0，最终日志无 Warning、Overfull、Underfull、缺字或未解析引用 |
| 页数与页码 | 6 个物理页：封面无可见页码，随后依次为 1、2、3、4、5；摘要 1 页 |
| 字体 | PDF 测得正文宋体 12 bp、一级标题黑体 14 bp、题目黑体 16 bp；所有 PDF 字体嵌入，见 字体嵌入检查.txt |
| 逐页视觉检查 | 六页均渲染并检查；官方标识、封面字段、摘要标签、三线表、跨页续表、矢量流程图、公式、引用和代码无可见裁切或重叠 |
| 独立复核 | 格式审阅代理核对官方原件，后续复核封面填写、摘要标签和源文件身份边界；没有代填人类接收结论 |
| 空白骨架 | 主文件独立执行三遍，退出码 0，日志无排版警告 |
| 可选开关 | 在临时副本同时开启目录和 AI 附录，三遍编译退出码 0，无排版警告；未改变用户的 AI 声明 |
| 模板持久性 | `python 协作/工具/verify_template_2026.py` 通过：三处模板字节一致、阶段资源同步不会恢复旧版、协作研读资料保留、模板源格式检查通过、演示内容未混入正式稿源码 |
| 工作流保护 | `python 协作/工具/verify_migration.py` 通过：缺少赛题/原模板仍阻断，重复初始化不重置状态；与 HEAD 对比除 profile 和更新时间外所有进度字段保持不变 |
| 版本锁 | `python 协作/工具/check_repository.py`：0 errors。skill-creator 的 quick_validate：Skill is valid |
| 原件完整性 | 官方下载文件 SHA-256 与原来源清单一致；用户桌面优秀论文原件未改动 |

最终 PDF 位于 `output/pdf/2026模板试编译.pdf`；哈希、完整命令、源文件哈希和视觉结果见 `preview-编译记录.json`。日志为 `preview-xelatex.txt`，页码标签为 `preview-aux.txt`，开关结果为 `骨架与开关测试.json`。

已修复 MiKTeX 宏包与旧内核不一致：安装 amscls，更新 ltxbase、latex-tools、hyperref、l3kernel，重建 XeTeX 格式。quick_validate 的 PyYAML 依赖仅安装到忽略的 `_tmp/skill-validator-deps/`，未加入正式项目运行依赖。

当前模板可开始填入真实论文内容；提交前仍需针对真实篇幅、图表、引用、身份信息、AI 使用事实和当届后续通知重新核验。DOCX 路线未在本次验证范围内。
