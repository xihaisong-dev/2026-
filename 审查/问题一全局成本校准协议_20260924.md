# 问题一全局成本校准与基础搜索机会审计

输入：opportunity-budget失败证据；本轮分支codex/q1-global-calibration。保持场景A、固定单核引用、seed0、每组12次机会及四基础粒度，不改正式默认。

开发8图058/039/072/009/100/040/028/067，2～5核。与routes_unguarded同版本同预算配对。新增routes_event_guarded保留相同候选池及局部排序，只将准入比较改为候选与当前解的固定三轮Pipe/依赖/DDR回放；无经验拟合系数、无按case分支、无阈值搜索。每次比较额外两次核内准备及代理回放明确计时，0次全局模拟，不包装完整评估为免费代理。

旧代理冻结每Task时长；新代理保留四Pipe顺序、算子和内存依赖，仅调整DDR操作时长，传播依赖延迟；三次松弛不是精确DDR全局仿真。校准指机制校准，不宣称统计校准完成。

预检查使用039四核、028四核、072两核的已知方案（开发数据），不将其视为独立验证。正式批次开跑前冻结代码、规则和以下停止条件。

记录被替代试探的方案hash、当时评分序号、是否已有精确评分及当前方案hash。与无新增路由的历史基础轨迹按hash匹配，计算被替代试探的直接改善和末尾有效候选是否缺席；轨迹分叉后只报关联损失，不将其虚称为单个动作严格因果效应。排除重复评分，将所有全局调用、参考复用和核内准备分开。

扩展条件：028/067所有8组相对固定全量基线不退步；内存三图12组与主导分量三图12组各自合计不劣于本轮未筛选对照。条件失败不改阈值迎合样本，不启动扩展；成功后按先前已固定16图010/021/026/027/035/043/044/046/054/057/068/078/079/083/097/098各2～5核跑两配置共128次。扩展后报告每例退步、时间与搬运量、逐图加速比均值；不事后逐例择优。

开发64次，扩展128次；服务器16workers。最终方案原版复核，或仅对字节相同且完整结果相同、输入/官方代码同哈希的历史原版验证作严格复用。该复用不增加搜索预算。保留日志及源码快照。不能把本轮24图说成全量100图。
