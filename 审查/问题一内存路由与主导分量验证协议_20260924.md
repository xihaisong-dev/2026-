# 问题一内存路由与主导分量验证协议

继承 b69c237 的 component_local_rank，先诊断后独立验证。不按 case 编号在算法中分支。

1. 诊断 case_058/039/072 的完整分量 LPT/Pipe 布局：测官方局部溢出、内存峰值和整体时间。候选总数有界，诊断调用不计作正式同预算收益。
2. 对 case_009/100/040：仅切分超过平均每核工作量的分量；沿该分量拓扑累计Pipe工作量在目标份额处切分，小分量完整保留。两种目标份额（1和1/2）构成至多两候选，不穷举边界。
3. 候选集和代理分数先生成，再读官方结果；正式挑战者最多一次新增结构评价替换普通扰动，保护既有四粒度预算，总机会12。局部官方准备单列耗时。
4. 正式对照上述六图加保护图028/067，2～5核、seed0；与冻结上一轮历史结果核对输入、参考基准、预算及最终原版复核。诊断后若提出新规则，须在独立输出与新冻结快照运行；不可把离线最优候选当算法成绩。
5. 对case_091用cProfile定位搜索瓶颈；精确优化必须保持候选轨迹、方案、官方时间和搬运相同。基线和改进版在同机、相同并发下计时；冷单核及最终原版复核单列。不用剖析器耗时直接计算速度收益。
6. 所有输出用新目录，源代码哈希固化，不改变正式默认和main，不覆盖既有运行。若某候选退步或耗时目标未达成，保留并明确报告。
