# D-003：2026 官方格式适配与写作核对

日期：2026-09-21。任务范围为用户要求的模板修订、试编译和每问写作与结果核对表；由本次任务串行写入，接收角色为 W，O 复核版本与环境，M/E 复核内容表设计。

## 变更

- 使用官方第二十三届封面及摘要抬头资产，封面西安交通大学标识和三名队员栏保持原样，去除 Word 转换出的封面页码 0。
- 重写旧混合赛事类中实际使用的排版部分：三号黑体题目、四号黑体一级标题、小四宋体正文、单倍行距、摘要起始页 1、连续页脚，无页眉。普通二三级标题不再放大。
- 默认关闭独立目录，正文不设置 30 页填充目标；官方规则与本项目内部质量规则分开记录。保留内容、数据来源、AI 声明和阶段输入检查。
- 正式 `main.tex` 只引用待填章节；演示内容由编译脚本临时注入，不进入正式源码图。新增三遍编译脚本与示例，覆盖封面填写、公式、引用、三线表、跨页表格、流程图和代码。
- 新增逐问写作卡、结果数值核对表与职责说明，均未代填实验或人工验收。

## 同步与持久性

仓库技能以原上游提交为基线，叠加 `gmcm-2026.1` 本地补丁；不宣称该补丁来自上游。版本锁记录原始修改文件哈希和当前哈希。两个技能模板源（初始化和 MANUSCRIPT 资源同步）与 `模板/当前竞赛/` 同步，避免下次 begin 恢复旧模板。

旧上游说明中强制目录、30 页正文目标属于历史规则，当前 GMCM/PDF 使用 2026 profile 和新来源说明，相关协议入口明确优先级。DOCX 导出器未作本次适配验证。

阶段资源同步会重建 `参考资料/` 和 `模板/`。因此将本任务前生成的研读成果与下载原件从 `参考资料/赛前研读/` 移至 `协作/赛前研读/`，原始下载字节不变，用户桌面的优秀论文原件未改动。正式论文编辑仍写入 `论文/`。

状态文件仅经 state_store 更新 `competition_profile` 与更新时间，并记录版本更新事件。阶段、完成事实、门禁和 AI 选择均未改变。

## 环境与验证

本机已有 MiKTeX XeLaTeX。为解决实际编译阻塞，安装 `amscls`，更新 `ltxbase`、`latex-tools`、`hyperref`、`l3kernel` 并重建 XeTeX 格式；使用 Windows 字体，没有把系统字体复制进仓库。`l3packages`、`l3backend` 已是当前仓库可用版本。

真实编译日志、页码标签与源文件哈希见 `审查/2026模板试编译/`。最终试编译 PDF 为 `output/pdf/2026模板试编译.pdf`，属于排版演示，不能作为比赛论文提交。早期失败评估保留在 `审查/赛前模板评估/`，用于追溯，不代表当前状态。

本次变更在本地任务分支保留，尚未由实际接收人接受，未代做 PR 审阅或合并。
