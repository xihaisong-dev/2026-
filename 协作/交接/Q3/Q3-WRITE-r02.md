# Q3-WRITE-r02：共享L2扩展模型与计算交付

## 交付信息

- 本地任务编号：Q3-WRITE-r02；用户直接指定任务，无关联Issue。
- A端：分析、建模、求解与核验；B操作者负责章节写作，C操作者负责实际渲染和格式复查。
- 交接状态：READY；500组成对官方评价、胜出方案重放、容量审计与图表已完成；未启动B/C，未代填接收。
- 工作分支：codex/q3-model-solve；计算产物提交07eb8b65（完整SHA以 `git rev-parse HEAD` 核对）；输入main f8931b56，问题二计算产物c47a5986。
- 物理工作区：C:/Users/Lenovo/.codex/worktrees/q3-model-solve；原工作区未提交工作保持原样。
- 正式阶段：DISCOVERY未启动，intake及阶段门禁NOT_RUN；本交付为实际研究与计算证据，不代替正式提交验收。2026-09-24 本地 9 项Q3测试通过，交付清单12项哈希及500行唯一配置检查通过；完整核验结果见 `图表/runs/20260924-A-q3-full-r02/verification.json`。

## B端论证主线

只编辑自己clone中的`论文/章节/Q3.tex`、该章引用和待澄清清单。继承Q2多处理器调度模型，展开共享FIFO缓存状态与DDR/L2双带宽池；同核跨子图驻留、500周期跨核释放与四Pipe规则保持。说明算法先使用Q2划分、映射与排序作为种子，在L2实际评价下作缓存事件引导的合法迁移/插入，不宣称穷举切图或求得全局最优。

正文顺序建议：场景B新增共享缓存的机制；FIFO查询/插入及独立带宽模型；Q2种子保护的有界搜索；100图五点硬件对照与调度对照；容量/带宽敏感性；边界与本问回答。不要把工作目录、版本号或内部状态字段当作学术模型名。

必须说明题面“用于复用多核共享输入”不是额外的原始输入专用限制：附录D.5和官方程序均以COPY_IN及逻辑tensor id为查询依据，包含spill reload与跨核中间张量。单核L2相对增益不能固定为1；case044/046的单核命中来源已经实际核对。

三种周期必须分开：问题二方案在B的无L2周期；同计划在Q3的L2周期；Q3新增搜索后的L2周期。共同单核分母的曲线用无L2单核；同核L2相对加速比逐例算T_B/T_L2再取100图算术平均，不得改用总周期比。`added_copy_bytes`是逻辑额外COPY量，不扣Cache命中。无L2命中率标“—（不适用）”；Cache命中率按字节计算。

容量与带宽扫描是诊断实验，全部主成绩使用固定1MiB、250 bytes/cycle。FIFO与内生时序不保证容量或带宽变化时周期单调；case005带宽反例、case044容量阈值均有真实重放。case044/046是在全量过程中为解释机制事后选取，不能冒称独立验证集。开发集3图对普通邻域及4/12候选预算的对照只支持局部结论。

## 格式与验收

沿用仓库2026 XeLaTeX/PDF格式与模型符号。图中文字和表头中文，周期为cycles、数据量为bytes，幅度足够时保留6位平均时间比。逐例数据分为周期表和搬运/命中表，避免压缩到不可读字号。最终图可引用计算交付的PDF，源数据在同目录CSV；诊断图不机械挤入正文。

B需交可编译源稿、实际页图、数值核对表、引用清单及待澄清项；C审查实际排版并给B修订反馈，再由C复查、A整合。计算预览不是正式参赛全文，不能据此声称最终格式、匿名或人工验收通过。

## 给B操作者的启动说明

你是B端问题三章节作者。先读AGENTS.md、正赛ABC协作协议和Q3-WRITE-r02，核对交付证据版本，按建模报告问题三节、计算结果及全量核验报告撰写论文/章节/Q3.tex。重点分开FIFO状态模型、Q2继承算法、同方案硬件收益和新增搜索收益；解释单核reload命中及容量/带宽非单调边界。只改指定章节和引用材料，不改代码结果，不代填A/C的验收。完成后交实际编译页和核对表给C。

## 接收确认

- B真人接收人、时间、检出版本：待填写。
- C审查与复查：NOT_RUN。
- 接收结论：待接收者填写。
