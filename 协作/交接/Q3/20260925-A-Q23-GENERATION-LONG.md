# 交接：Q23-GENERATION-LONG — 生成复用长预算收益验证

## 交付信息

- A提交，接收后续A；2026-09-25（+08:00）；READY仅表示本轮验证完成。
- 分支codex/q23-generation-long；产物提交c79d777bda3aba8bcce43a564eff32721cbe29e7；输入3da725c736e14ea5d72566c0f591f2c44b834609。未推送，无新PR/Issue。
- 本机C:/Users/Lenovo/.codex/worktrees/q123-manuscript；服务器/home/xihs/q1-campaigns/20260925-q23-generation-long。
- 提交冻结codex/q23-submit-freeze（ebce54dd）未变。正式阶段门禁NOT_RUN，未更改状态源或正式500配置。

## 实施与证据

沿用已测GenerationReuse，无求解源码修改。Q2选033/067/072，Q3选005/067/072，两臂12项，只切换生成复用，准备缓存始终关闭。普通joint规则、初解、种子0固定；两臂均590秒且含最终原版重放/物理审计；两臂提议上限同时提高至384，避免旧96上限截断预算。不是与旧96短试验作同预算比较。

服务器12并发，12项全部有效完整结束、无超时截断。最长外部墙钟483.965秒、峰值RSS593.871MiB。797共同提议前缀一致，428评分完整哈希对照一致，实际评分428→518。共同前缀生成耗时下降31.58%～86.41%；单种子共享服务器墙钟含负载噪声，不宣称统计显著性。

- Q2三图周期/搬运全部持平，样本均值4.930313032不变。
- Q3为1胜2平，总周期17,737,252→17,736,825，样本均值4.070983154→4.078699481，额外搬运增加2208字节。
- 唯一周期改善为Q3/005：42,211→41,784，下降427周期（1.0116%）。基线107提议，复用143提议；接受改进发生于基线未覆盖的零基索引109、111、125、131、140。共同前缀一致，证明本次收益来自额外搜索机会。
- Q2/033均完成384提议、同一结果352040周期；求解耗时358.177→263.869秒，下降约26.33%，不是NPU周期下降。

报告和原始证据：图表/runs/20260925-A-q23-generation-long/README.md、report.json、contract.json、jobs、rows、hashes.json、validation.json。原始138文件下载与Git索引哈希通过，服务器300源码/输入与本机一致。程序/code_manifest.json、图表/全部结果.json、计算结果.md记录本轮实验。

## 复现

服务器Python3.12.14，运行命令已完成：

```sh
nohup /home/xihs/q1-runtime/run-python -u run_generation_long.py > generation_long.log 2>&1 < /dev/null &
```

脚本和日志归档在证据目录。复跑用新副本/新输出，保留合同、程序、官方依赖、Q2初解和上一轮Q3种子；不能覆盖现有rows/jobs。每项真实命令保存在rows/*.json.command。

本机实际报告复算和编译检查退出码0：

```sh
python -m py_compile 程序/q23_generation_long_report.py
python 程序/q23_generation_long_report.py 图表/runs/20260925-A-q23-generation-long
```

求解源码沿用上轮已测版本，本轮没有重复运行并冒称新的11项单测。最小产物检查通过，正式阶段门禁仍NOT_RUN。

## 决策与限制

生成复用已经出现局部长预算周期收益，但六组只有一组改善，尚不足以推广整个组合。后续可在完整Q2 protected/Q3 combined中单独接入，仍保护候选与预算规则并做配对验证。当前是普通joint控制变量试验，不是完整组合的新成绩；不跑新100图、不替换冻结提交版。

初解历史生成不计，仍是暖启动；没有证明冷启动十分钟全面达标。小样本平均值不能替代全100图均值。缓存生效范围是同一不可变图作用域，完整参数键及隔离副本要求不变。正式提交补齐从冻结分支开展，实验分支不冒充提交验收。

接收人自行填写验收结论，未代填ACCEPTED。没有等待用户批准的阻塞项。
