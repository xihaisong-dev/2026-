# 交接：Q23-CACHE-SEARCH — 准备缓存接入真实限时搜索

## 交付信息

- 提交人A；接收后续A；2026-09-25（+08:00）；READY仅表示本轮实验和证据完成。
- 分支codex/q23-cache-search；产物提交63288ce16a97bf60bd3afd34a64f987796fa02e4（主体9bade0b8df504a4721c1e21231b301a957c928bf）；未推送。
- 输入提交aa2047a2，承接准备缓存r2b验证。无新Issue，使用本地任务编号。
- 本机C:/Users/Lenovo/.codex/worktrees/q123-manuscript；服务器/home/xihs/q1-campaigns/20260925-q23-cache-search。
- 阶段门禁NOT_RUN，不修改状态源、正式入口、旧500配置或论文。未启动新100图；20个服务器任务均已结束。

## 已完成与产物

- 程序/q23_cache_search.py：固定普通joint提议策略、初解和种子，按已接受方案继续真实搜索；只切换软件准备缓存。每次候选仍重新计算全局DDR/L2；最终关闭包装器，原版重放完整结果并检查物理内存，Q3另做FIFO审计。
- 准备缓存复用上一版SerializedPreparationReuse，32MiB序列化负载上限；不改变Q3硬件Cache开关。
- 图表/runs/20260925-A-q23-cache-search/：冻结合同、种子、逐任务方案/完整评价/搜索日志、两臂报告、服务器启动脚本、哈希及验证记录。
- 程序/code_manifest.json、图表/全部结果.json、计算结果.md：新增实验记录，正式成绩未替换。
- .gitattributes：冻结目录保持原始字节。230个服务器原产物哈希已在磁盘和Git索引核对；287个服务器源码/输入哈希与本机一致。

## 结果与决策

服务器12并发，完成Q2四图033/065/067/072和Q3四图005/044/067/072的开关对照，共16任务；另做Q2/065、072及Q3/044、072四项冷启动压力测试。小图90秒、大图590秒，种子0，最多96提议。20项全部有效、完整结束，均完成最终原版复核；540共同提议前缀一致，其中336次评分完整结果哈希一致。最长外部墙钟484.550秒，最大RSS638.063MiB，无真实任务触发超时。

Q2小样本平均加速比4.888048141→4.888105118，总周期18,579,665→18,579,649，仅少16周期，额外搬运增加6144字节。Q3平均加速比4.117209220、总周期17,773,903、额外搬运352,279,588字节全部持平。不是全100图成绩，不可替换论文数值。

冻结扩量要求两问均有严格样本收益，本轮Q3不满足，因此不启动新100图。没有增加关键候选，也不默认推广此缓存到所有图。

## 复现和检查

服务器Python3.12.14，通过/home/xihs/q1-runtime/run-python执行；实际启动命令：

```sh
nohup /home/xihs/q1-runtime/run-python -u run_cache_search.py > cache_search.log 2>&1 < /dev/null &
```

该脚本已归档到实验目录。重跑必须在独立副本中进行：实验输出目录仅保留contract.json和seeds，保留项目的官方评估依赖、输入与Q2初解目录，再把归档启动脚本放到项目根。不要对现有证据目录再次启动：子进程拒绝已有输出，但启动器会重写统计文件。

本机实际验证（全部退出码0）：

```sh
python -m unittest discover -s 程序/tests -p "test_q23_cache*.py" -v
python -m unittest discover -s 程序/tests -p test_q23_preparation_reuse.py -v
python 程序/q23_cache_search_report.py 图表/runs/20260925-A-q23-cache-search
```

前两项分别3和5个测试通过。超时测试包含无检查点失败和合成故障注入后的检查点保留；故障注入只验证监督器，不冒充官方真实数值验证。报告复算保留全部持平和退步的评价耗时。最小交付文件检查通过，正式阶段门禁仍NOT_RUN。

## 计时口径和局限

预算从创建求解子进程前开始，包含读取、建模、候选生成/评分、最终原版重放/内存审计。外部墙钟还包括解释器启动和关闭。暖启动只读取历史高质量初解，无法据此证明其生成耗时；冷启动用本实验的新Pipe分量初解，包含生成，但不同于旧Q1→Q2→Q3串联入口。固定单核T1只是计分参照未重新计算。本轮不能证明全100图、1～5核或原正式入口冷启动均满足10分钟。

共同96提议上限和20%保守复核预留会限制有效搜索长度；本轮是普通joint控制变量实验，不是完整组合求解器对照。共享机器单轮墙钟有噪声，不宣称统计显著提速。

## 下一步

1. 先检查072缓存零命中、各204次淘汰的工作集与替换策略；067命中率也只有约13.77%/17.75%。不要直接假设增大容量即可提速。
2. 检查昂贵重复生成：Q2/072开启臂40/55提议重复，约117.45秒；Q3/005为5/15、39.06秒。优先复用生成准备或提前识别等价动作，同时保持候选规则与顺序可审计。
3. 若调整缓存或生成准备，再冻结同样对照并在共同候选前缀比较完整结果；稳定收益后才讨论全100图及正式入口整合。

接收人自行填写验收结论，未代填ACCEPTED。没有需要用户额外批准的阻塞项；停止扩量是未达到已冻结实验收益条件。
