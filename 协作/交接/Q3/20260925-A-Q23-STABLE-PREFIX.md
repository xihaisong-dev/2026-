# 交接：Q23-STABLE-PREFIX — 保护基础候选次序

## 交付信息

- A交后续A，2026-09-25（+08:00），READY；无新Issue/PR，未推送。
- 分支codex/q23-stable-prefix；产物提交b710703f532bcfe1310f430816c20b600a8b7b36；输入271a70e2。
- 本机C:/Users/Lenovo/.codex/worktrees/q123-manuscript；服务器/home/xihs/q1-campaigns/20260925-q23-stable-prefix。
- 冻结分支codex/q23-submit-freeze（ebce54dd）未变。原protected/combined及正式入口与冻结版无差异。正式门禁NOT_RUN，未改状态源或正式成绩。

## 实施与证据

程序/q23_stable_prefix.py建立独立实验策略，保留原初解菜单。Q2先4次joint，Q3先4对cache/joint，再按结构先验固定轮转各适用候选类别；基础首轮后继续同一周期。实际耗时只影响停止，不能改变类别权重或RNG轨迹。确定性保证的是新序列复用开关的共同前缀，不是旧耗时自适应策略的历史轨迹。

三臂：旧策略、新序列、新序列加排序准备复用。全部启用原生成复用，590秒含20%最终复核预留、5核、种子0、384次提议上限；旧策略也采用384，不能直接与之前96次上限作因果比较。Q2为033/072，Q3为005/072，共12项服务器并发运行。

- 12项全部通过原版完整重放及物理审计，最长479.844秒、峰值子进程RSS917.50MiB。
- 808共同提议、接受决策均一致，426共同方案的完整官方结果哈希一致；复用臂四组均覆盖固定序列基线，未重演耗时改变导致的路径分叉。
- 评分426→428，但新序列的复用开关四组周期和搬运全部持平。Q2/072为8→9提议、19→20评分；Q3/072为32→33提议、27→28评分。Q2/072尚未完成整个基础首轮，不能把新增的一步全部称为进入追加阶段；它是同一序列更长的前缀。
- Q2/033和Q3/005均完成384次提议，额外时间未再越过统一上限。
- 相对本轮旧策略：Q2/033 355089→352952（−0.602%）；Q2/072 4811152→4789700（−0.446%）；Q3/005 42856→43163（+0.716%）；Q3/072 4771746持平。
- 旧Q3/005经cache/joint多次改善后，最终insert达42856；该方案未进入新序列评分池。固定轮转仍存在搜索机会损失，不能推广替换冻结版。

证据目录图表/runs/20260925-A-q23-stable-prefix含合同、三臂逐项命令、完整方案与官方结果、搜索轨迹、报告、来源及字节哈希。251原始文件下载及Git索引哈希通过，307服务器源码/输入与本机一致。程序/code_manifest.json、图表/全部结果.json、计算结果.md登记本轮。

## 复现与检查

服务器Python3.12.14，依赖入口/home/xihs/q1-runtime/run-python。已执行：

```sh
nohup /home/xihs/q1-runtime/run-python -u run_stable_prefix.py > stable_prefix.log 2>&1 < /dev/null &
```

归档脚本负责固定哈希及资源检查；逐项命令见rows/*.json.command。复跑使用新目录，不覆盖已有rows/jobs。

本机命令退出码0：

```sh
python -m unittest discover -s 程序/tests -p test_q23_stable_prefix.py -v
python -m py_compile 程序/q23_stable_prefix.py 程序/q23_stable_prefix_report.py
python 程序/q23_stable_prefix_report.py 图表/runs/20260925-A-q23-stable-prefix
```

本机测试1项2.955秒，服务器同项4.135秒。测试包含两个问题的完整入口，验证共同提议、追加阶段、原版重放一致。最小产物检查与git diff --cached --check通过；正式门禁NOT_RUN。

## 决策与后续

保留实验，不推广、不更新全100图。机制层面前缀保护通过，性能层面追加两次评分未产生收益；固定规则相对旧策略两胜一负一平。暖初解历史生成不计，未证明冷启动十分钟全面满足。

下一步可比较按改善量/尝试次数调整的确定性权重，同时保留基础序列与最低探索机会。不要恢复瞬时耗时权重，也不要根据这四组case编号记忆选算法。此方案本轮未实现或验证，应另立同预算对照。

接收者自行填写验收结论，未代填ACCEPTED。无待用户解除的阻塞项。
