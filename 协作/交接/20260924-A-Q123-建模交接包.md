# 三问建模与计算证据交接（A → B/C）

- 状态：READY，待 B 真人接收；C 在 B 交稿后审查，不代填人工验收。
- 版本口径：Q1 默认 r02；Q2 采用 r07；Q3 采用 r02。三问在同一输入、固定硬件配置和官方评估器上衔接。Q3 计算提交为 `07eb8b65`，本交接单自身在后续独立提交；收到包后核对 `版本与校验.json`。
- 来源：`题目/赛题原件/通用神经网络处理器下的多核调度问题.docx`；无损处理输入为 `数据/processed/q1/`，其 `manifest.json` 保存原附件及各输入/官方程序哈希。
- 正式工作流：仍为 DISCOVERY/initialized；官方论文模板导入、AI 使用说明选择、B/C 实际章节及最终 PDF 验收尚未完成。本包是建模与计算交接，不是竞赛最终提交声明。

| 问题 | 模型和实际算法 | 结果与核验入口 | 规定/扩展指标 |
| --- | --- | --- | --- |
| Q1 场景 A | 有向无环图划分与多核调度；结构感知初解、关键路径和空隙调度、有界邻域搜索，周期优先 | `审查/问题一交付与赛题要求核对_r02.md`；`图表/runs/20260924-A-q1-delivery-r02/`；`程序/q1_submit.py` | 100 图平均加速比 1、1.834917、2.546238、3.161009、3.665903 |
| Q2 场景 B | 继承图划分/多核调度，加入同核跨子图复用、跨核同步及真实张量生命周期；采用 r07 有界搜索 | `审查/问题二r07计算提交与复现.md`；`图表/runs/20260924-A-q2-delivery-r07/`；`图表/runs/20260924-A-q2-full-r07-shared-r08/`；`程序/q2_current_submit.py` | 100 图平均加速比 1、1.981113、2.815714、3.550822、4.159974；400 个 2～5 核方案官方回放，另有 100 个规定单核基准 |
| Q3 场景 B + 共享 L2 | 继承 Q2 种子，加入 FIFO 状态与 DDR/L2 独立带宽的离散事件评价；缓存事件引导的有界迁移/插入 | `审查/问题三共享L2模型与百图求解.md`；`图表/runs/20260924-A-q3-full-r02/verification.json`；`图表/runs/20260924-A-q3-delivery-r03/pairs.csv`；`程序/q3_submit.py` | 500 组成对无 L2/L2 官方评价；同核平均时间比 1.008622、1.002467、1.006671、1.014923、1.022567 |

上述“平均”都是每图时间比的算术平均，不能替换为总周期之比。Q3 的 `added_copy_bytes` 是逻辑额外搬运量，不因 Cache 命中自动扣减。Q1 最新 FIX-1 是独立实验分支，未并入本套 Q1 默认 r02 和 Q2/Q3 的继承种子，不得混用 3.679114 与本包 Q1 曲线。

## 接收后先做的验证

1. 解压压缩包，在根目录运行 `python 程序/主程序.py prepare`（若无损输入已存在则检查其哈希）；核对 `数据/processed/q1/manifest.json`。
2. 检查 `图表/runs/20260924-A-q1-delivery-r02/all_case_results.csv`、`图表/runs/20260924-A-q2-delivery-r07/selected_500_rows.csv`、`图表/runs/20260924-A-q3-delivery-r03/pairs.csv` 各 500 行、配置唯一。
3. 运行 `python -B -m unittest discover -s 程序/tests -p 'test_q3*.py' -q`；已实测 9 项通过。Q1/Q2 的历史测试及独立复现口径见逐问审查报告。
4. Q3 全量重算会消耗大量计算资源；已有 500 组官方评价、1000 份 FIFO 事件账本、500 份胜出方案全局内存审计和逐配置核验记录。写作时优先核对冻结证据，不把重新全量运行当作打开包的前置条件。

## 写作与接收边界

先读 `问题分析.md`、`建模报告.md`、`计算结果.md`，然后按 `协作/交接/Q1/Q1-WRITE-r02.md`、`协作/交接/Q2/Q2-WRITE-r07.md`、`协作/交接/Q3/Q3-WRITE-r02.md` 分问撰写。B 只改自己的章节与核对材料；C 根据实际渲染页审查格式；A 最终核对三问符号、分母、图表与附件入口。Q3 有 4 组优化后仍比无 L2 慢，Q2 个别核数增加会变慢，均须如实呈现。不得把官方仿真周期当作硬件实测，或声称全局最优、全量端到端 10 分钟保证。

接收人、接收时间、实际检出 SHA、验证命令、结论与待补事项：**待 B/C 真人填写**。
