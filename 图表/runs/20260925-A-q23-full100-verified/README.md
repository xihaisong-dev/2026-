# Q2/Q3 五核全100图批次

状态：已启动，尚未完成。启动时200项入队、15项开始，无已观察到的worker错误；实际进度以服务器progress.json为准，不以本页静态启动快照代替。

组合：Q2 protected、Q3 combined，双方开启生成复用；新奖励权重和排序准备复用关闭。每项590秒，五核、种子0、384提议上限，20%最终官方复核预留。相对于早期96提议上限的正式批次，本轮预算口径不同，不得仅凭结果变化宣称某机制的因果收益。

Q2沿用固定Q1迁移初解；Q3沿用固定最新Q2初解与Q3锚点，不读取本批Q2结果。完整官方重放和物理审计在590秒内；历史初解生成与固定单核基准生成不计。不宣称冷启动端到端十分钟达标。

服务器目录/home/xihs/q1-campaigns/20260925-q23-full100-verified。运行入口程序/q23_full100_verified_run.py，配置contract.json；rows为逐图真实命令与有效性，jobs为方案与完整评价，progress.json为明确标注分母的进度，complete.json为结束标记，结束后生成证据ZIP。无效/超时不自动混入旧结果，不重试覆盖目录。

完成后须拉取结果、核对哈希、检查200项完整官方复核，分别按100张图T1/T5的算术平均统计Q2/Q3平均加速比，再比较总周期与搬运。Q3最终同方案无L2对照尚不由本启动器生成，如用于提交补齐需另列生成和核验耗时。当前不替换冻结版、不修改正式成绩，正式门禁NOT_RUN。
