# 问题一同候选池事件排序、核内准备复用及全量协议

1. 继承24图同预算实验与场景A。候选生成、去重、合法性、最多三布局、基础粒度及12机会固定。事件排序替换池内local分数，按原生成序号破平局，已有分组多样性第二候选规则保持。新增排序适用于eligible完整分量、memory和hybrid的原有池，不增加池大小。
2. 排序与准入使用既有三轮Pipe/依赖/DDR代理。复用完整partition映射（含子图ID）下的核内准备，缓存最多2个partition；布局预测缓存最多8个完整plan，含核心和顺序。每次完整评分仍执行全局事件循环且计预算，缓存不复用全局评分，不增加机会。核内数据深拷贝隔离；官方输入文件不修改。
3. 开发8图2～5核：routes_event_rank vs routes_event_reuse（64次），核验完整方案、结果、搜索轨迹与原版一致；并对上轮routes_event_guarded做算法效果比较。扩展16图routes_event_reuse（64次），与上轮同输入同预算的event_guarded比较，分开汇报，不宣称历史求解墙钟严格可比。上一轮与本轮同池候选名单一致需检查。
4. 开发、扩展均要求总执行时间及各核逐图平均加速比不退步，且028/067保护组无退步，缓存等价与跨平台检查通过，则冻结routes_event_reuse跑100图。若排序条件失败，不能逐case择优：固定回退routes_gate_reuse（旧局部池排序+事件准入+核内复用），先核验其等价性后跑100图；不得继续调参数追逐同一验证集。
5. 两主机按JSON互斥case清单划分。服务器另跑009两核event_reuse作为明确标注的跨平台诊断，与本机开发结果比较计划、完整结果、预算及搜索轨迹；它是额外1次诊断，不混正式矩阵。Python3.14与3.12结果不一致时停止全量并定位。
6. 全量前生成带哈希的400配置台账。若选中event_reuse，前24图相同代码/参数/种子的96份已核验记录可原样纳入，不重跑；其余76图按算子数分配，小30图给本机，其余服务器。若回退则新增400份。全量统计必须每case每core唯一，不按成绩择主机、择方案或拼最优。
7. 100图固定官方单核基准；1～5核平均加速比为逐图T1/Tk算术平均。逐例输出Makespan、额外搬运、运行时间及验证谱系。复用历史最终原版验证仅限同输入、同plan字节、完整结果相同且来源曾实际原版验证，不能链式复用。新方案必要时原版重算。允许复用严格一致的评价证据节省时间，不把复用算作新算法收益。
8. 本机可用内存约2.5GB，2workers；服务器14workers，保护其他运行。本轮不自动更新main或正式默认；保存冻结源、真实命令、分片台账、失败和哈希。不改变正式阶段或人工门禁。
