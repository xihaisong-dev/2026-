# 问题一：题意、实验口径与研究过程

本文件前半部分交接已经实现并完成全量测试的问题一研究原型，不代表正式阶段门禁、论文或人工验收已经通过。文末新增问题二当前 main 首轮研究；问题三不在本轮范围。

## 1. 场景 A 的约束

一个子图对应一个 Task；对所有非 COPY_IN/COPY_OUT 操作分配子图，并给出每个核心上的子图执行顺序。跨 Task 的私有缓存状态不保留，所以跨子图数据必须经过 DDR，即使相邻 Task 被映射到同一核心也不能免除搬运。Cinner=Ccross 表示通信路径的单位代价一致，不表示同核与跨核的任务释放等待完全相同。

划分需要同时权衡计算并行、重复输入读取、四条 Pipe 的负载与重叠、L1/UB 容量压力及共享 DDR 竞争。最终目标首先是官方多核评估的 Makespan，额外数据搬运量用于次级比较；不能用跨核边数乘同步常数替代 Makespan。

## 2. 固定评测口径

共 case_001～case_100。单核参考是每张整图在给定核内启发式上的固定结果；不以搜索后的单核最优方案更新分母。每张图的 k 核加速比为 T_i1/T_ik；正文所用平均加速比是 100 个比值的算术平均。单核点固定为 1，不能使用单核总时间除以多核总时间代替平均值。

正式采用的研究结果是第十八轮 shared_region：种子 0、每配置 12 次评价机会。100 个单核结果加 400 个多核结果构成 500 行交付数据。另有 shared_resource 的 400 行初始划分对照，所以原始汇总包含 900 行；不可把这 900 行混合取平均。

原始结果：`图表/runs/20260923-A-q1-v18-merged/`。原始 CSV：`图表/runs/20260923-A-q1-v18-analysis/all_case_results.csv`。写作专用 500 行筛选与五点表：`图表/runs/20260923-A-q1-writing-handoff/`。

## 3. 为什么先用少量 case

初期 case_001、case_093、case_034 用于输入输出、合法性、调度与成本反馈的快速工程验证。后续的 001、002、034、078、093 是开发调参集，不是预先注册的独立测试集，不能仅凭其改善声称全部图都改善。

第七轮通过全量静态结构画像，按操作规模分层，并结合关键路径比例、搬运/计算比和扇出选择未测试样本；分层代表包括 065、017、077、092，另加入搬运占比较高的 045、关键链占比较高的 048。092 在当时的短时上限内未完成，不能计入完整数值对照。因此当时真正完成的五图开发对照是 017、045、048、065、077。后来又用 006、017、040、045、048、065 做机制诊断。小样本的意义是降低每轮修改、定位错误和消融的周转成本，其代表性与超时选择偏差必须披露。

第十八轮补齐全部 100 图，避免只展示开发样例。该轮仍是固定种子的一次全量实验，不等于多种子稳健性证明或独立盲测。历史依据见 `计算结果.md`、第七轮与第十八轮审查报告。

## 4. 本轮结论及后续优先级

新初始划分 shared_resource 相对 shared_region，在 400 个多核配置中为 23 胜、206 平、171 负，因此保留原方案，不逐 case 挑选两套方法的最好结果冒充一个统一算法。

下一步先根据 100 图画像区分关键链释放等待、DDR 重复读取/拥塞、主导 Pipe 和并行空间限制，再针对真实可改善部分投入预算。低加速比本身不证明存在巨大可优化空间；最终没有溢出搬运的方案不能以减少溢出作为首要解释。候选排序先做冻结成本状态下的误差与选择遗憾诊断，再以同预算、多种子、保留基础划分机会的对照验证联合细化。

求解程序耗时与生成方案的执行时间必须分开：本次索引与缓存加速保持方案和官方数值不变，解决的是等待计算结果的耗时，不是又降低了 NPU 任务 Makespan。


## 问题二：基于 Q1-WRITE-r02 的规则复核（2026-09-24）

基线提交为 main 50b91d46a7bd43ca404c1285dfc27929496bf266。旧 Q2 r01–r05 位于原工作目录，作为历史机制证据，不直接继承成绩；当前独立分支 codex/q2-main-r02。Q1 冻结模型、500 行结果及程序不改。

| 维度 | 问题一 A | 问题二 B | 原始依据 |
|---|---|---|---|
| Task 与子图 | 一子图一 Task | 同核全部子图合并一个 Task | 原题 1.5、问题2、附录D.2；B `_build_scene_b_tasks` |
| 同步 | 同核 Task 前驱完成+100，跨核 Task 前驱完成+1000 | COPY_IN 必须等源 COPY_OUT 完成+500；不是等待整个源子图结束 | 附录D.2；A evaluate_scene_a；B external_release |
| 跨子图通信 | 全部经 DDR，含同核 | 同核直接复用，跨核 tensor 按实际源核—目标核生成一对 COPY；直接 Op 边分别重建 | 1.5；B 140–235 行 |
| 缓存 | Task 间清空 | 核内跨子图驻留，最后消费完成释放；不足时 spill/reload | 1.5、附录C/D；B 248–267 行、schedule_step2/3 |
| 核内并行 | Task 内多 Pipe | 同核合并 Task 内四 Pipe 可重叠；每 Pipe 固定顺序、单槽位 | 附录D.3/D.4；B queue_if_ready/issue |
| 输入/输出 | Task 边界 COPY 与重复输入 | 每消费核读入；输出保留写回；同核不等于没有搬运 | B 159–179 行 |
| 共享资源 | DDR 总带宽共享 | 同样共享 DDR，依赖由 COPY 释放，竞争随活跃传输数变化；没有 Q3 的 L2 | 附录D；B advance_ddr_work/reschedule_ddr |
| 决策/格式 | node_to_subgraph、core_schedules | 同样两个字段；只给非 COPY 原操作分组，容量和核内操作序列由官方生成 | 附录B；contest_io/validation |
| 评价 | Makespan 主要、额外搬运兼顾 | 问题2要求缩短 Makespan 并减少额外搬运；加速比定义明确与问题1相同 | 问题2正文，附录B |

单核分母必须重新由当前官方核内整图算法计算/严格校验等价，不把 Q1 多核成绩当成 Q2 成绩。平均为逐图 T1/Tk 的算术平均。题面没有指定两个指标的加权系数；本轮算法以周期优先、同周期少搬运作为显式求解约定，同时完整报告额外搬运退步，不冒称题面给定唯一词典序。

### 复用审计

- 可复用：q1_io 的输入审计、Graph 的依赖/拓扑/商图无环检查、q1_structural_seeds 的分量/链/深度分组、q1_memory_routes 的有界主导分量切分；结构来源注明用户参考工程，非原创声明。
- 仅复用结构：hybrid_pool 的 node_to_subgraph，丢弃 A 的映射与顺序，按 B 的增量通信和流水线负载重新放置。分量负载平衡不含 Task 等待，可直接作为提案。
- 必须重建：CostGraph 的逐 Task 局部时长观测、A 的边界通信成本、q1_insertion 的整 Task 空隙/等待公式、q1_fast_evaluator 的 A 事件引擎；不能改常量就声称等价。B 以核级任务准备和操作级时间线评价。
- 可迁移工程思想：增量合法性、去重、有界区域和预算保护。B 缓存仅在本求解上下文，键含问题 B、输入、配置、延迟、官方代码哈希、适配器版本及完整计划；容量2，返回隔离副本。不同场景成绩不可复用。

### 参考工程和文献的作用

参考工程多粒度候选很有价值，但同样需要官方 B 选优。当前 main 已吸收其结构生成，不应重复包装成新模型。P01（PDF9–11页）支持保持商图无环的局部划分调整；P13（PDF7–11页）支持通信亲和与等待机会成本，但其在线调度/缓存加速假设不能直接照搬。P14（PDF3–5页）基于固定总时长串行模型，不能证明降低驻留面积必然降低本题 makespan。P03（摘要与模型部分）提示内存与并行度冲突，不能直接改输入 DAG。

paper/2607.17422v1.pdf 的 DAN-Scheduler 摘要及导论明确共同优化核内拓扑顺序、内存布局和流水线重叠；本题前两者由官方 Step1/2/3 确定，不允许替换它们。其价值是指导合法的外层划分/映射/子图序列调整，不沿用论文的性能增幅或内存重排机制。P12 只有元数据，不作为公式证据。

### 第一轮实验（运行前冻结）

开发 006/048/050（含 Q1 退步图006、单连通混合图、已有 J 正例）；验证 012/039/072（不同结构与容量压力，当前规则未调参，Q1 历史已知）；大图 014/091 的2/5核。小图2–5核，单核固定为1。确定性 seed0，无随机参数拟合。每组8基础机会完整保留，加4次普通 J 或引导 J，同为12；重复缓存仍计机会，额外最终重放独立记账。

先评价 Q1 r02 原方案；不合法则记录失败，采用整图单核放置合法回退。基础候选包含完整分量、共享输入顺序、主导分量切分、链与深度窗口。J 在已有结构下联合调整核心与合法插入位置，本轮不同时叠加 L 或重划分修复。分别记录迁移、结构基线、普通 J、引导 J；结构基线对迁移不是同预算因果消融，不能误称单项算法优势。

预设引导 J 采用门槛为验证几何平均比普通 J 至少提升0.5%、至少两图改善、各核平均提速不下降、大图全部合法且基础预算无挤占。未通过则不把引导 J 推为默认、不启动其百图实验。即使优于普通 J，还须与参考 B 工程作外部差距审计；不同预算的历史比较不能当成公平算法胜负。


## 问题二 r03：两项隔离对照

继承r02的B模型，不增加模型名称。开发012/048/050，新确认018/033/067，2至5核，单核固定分母，seed0。先冻结源码、样本、配置和评价器，确认集不参与改规则；Q1历史已知，不宣称外部盲测。

初解对照各8机会：Q1计划B复评、整图回退、6结构候选。固定划分、rank、持续时间估计、核心时间框架及决胜规则，只比较参考B HEFT的边字节估计与按生产核—消费核去重的内部通信估计；直接Op边逐边计费，外部输入/输出局部成本保持不变。因此只检验这项跨核通信修正，不声称覆盖全部通信误差。

J对照固定参考HEFT的同一8个基础候选，各加4个修复机会：普通前4、普通前2加引导前2、引导前4。混合重复候选仍占名额、不补位，最终方案保留基础解。两类对照分别判断，不把8和12次机会混作同预算比较。

采用条件：确认集逐配置提速比比值的几何均值至少1.005、至少两图改善、各核平均提速比不下降、合法性和回放校验全通过。不通过不替换默认，不启动百图。所有成绩、额外搬运和搜索/回放/固定分母耗时分开记录。
