# 问题一初版程序验证

本文件是原型验证记录，不代表正式 COMPUTATION 阶段完成或全量实验结论。工作流状态保持 DISCOVERY / ready；未推进门禁、未派发 B/C 写作任务。

## 输入与实现

- 输入：用户提供的多核调度附件 ZIP，来源 SHA-256 为 `3112331344df7dcf2dcef2ff5f789669c0419e32cefbbc6e2742b9e36d5c43c9`。
- 100 张原始图已逐一通过附件 `validate_graph`；无损提取至 `数据/processed/q1/`，数据、配置及官方评估代码均有哈希。
- 入口：`程序/主程序.py`。三种方法共用 `程序/q1_solver.py`；只使用 Python 标准库。
- 对每个候选使用官方数据依赖与同核顺序联合检查；最终成绩来自未修改的 `evaluate_scene_a`。
- 固定 DDR 带宽 60 bytes/cycle，L1 524288 bytes，UB 131072 bytes，同核 Task 间隔 100 cycles，跨核依赖等待 1000 cycles。

## 实际命令与证据

```powershell
python 程序/主程序.py prepare
python -m unittest discover -s 程序/tests -v
python 程序/主程序.py solve --cases case_001 case_093 case_034 --cores 1 2 4 --method alns --budget 4 --output 图表/runs/20260923-A-q1-v1-smoke
python 程序/tests/check_scale.py 图表/runs/20260923-A-q1-v1-scale
git diff --check
```

最终功能验证证据：`图表/runs/20260923-A-q1-v1-smoke/`。随机种子为 0；ALNS 每个组合最多 4 次新候选官方评估，初解评估另计。目录保留方案、完整官方评估、搜索轨迹和源代码哈希。

| 图 | 非 COPY 算子 | 单核 cycles | 2 核 cycles | 2 核加速比 | 4 核 cycles | 4 核加速比 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| case_001 | 600 | 233110 | 119153 | 1.9564 | 71772 | 3.2479 |
| case_093 | 552 | 74205 | 47688 | 1.5561 | 33524 | 2.2135 |
| case_034 | 3745 | 842996 | 492478 | 1.7117 | 445271 | 1.8932 |

以上是 3 图 × 3 核数的冒烟验证，不是 100 图平均成绩。核数为 1 时使用完整图单 Task 基准，加速比为 1。各图最终方案均不劣于作为兜底的完整图单 Task 方案。

9 项自动测试覆盖：共享 tensor 按消费子图去重、同核边界搬运和 100 周期间隔、跨核 1000 周期等待与传输并存、联合核心顺序成环、非凸收缩成环、三算法的合法性与确定性、单核口径、输入哈希篡改，JSON 文件跨平台 LF 哈希稳定性，以及宽图过度碎片化回归（相关项目合并在同一测试方法中）。

最大图 `case_014` 含 35705 个非 COPY 算子，最终构造与合法性证据单独保存于 `图表/runs/20260923-A-q1-v1-scale/`：贪心 558 个 Task，多层次 476 个 Task。此检查只验证覆盖、子图 DAG 和联合核心顺序合法性，不声称已完成时间仿真或缓存执行验证。

## 发现、修正与限制

- 初次大图分组产生约 2.1 万个 Task。已改为全局优先队列与数据邻域结合、控制每块规模，并加入宽图回归测试。
- 缓存的活跃集估计和 Pipe 负载仅用于候选排序；真实缓存溢出、地址依赖及共享带宽竞争由官方评估器处理。
- 多层次方案在部分样例不及贪心，因此保留多初解择优，未预设多层次必然占优。
- 最大图的完整评估尝试耗时较长，已主动停止；两次中断目录为 `20260923-090657-774703-A-q1` 和 `20260923-091329-394417-A-q1`，没有 completed=true，不是有效成绩。早期完成的小图调试记录同样保留本地，不替代本文件指定的最终验证版本。
- 全量 100 图 × 2～5 核、等评估预算对比、多随机种子和消融尚未执行。ALNS 初版每次组合 1～3 次局部编辑，尚未实现按实际关键瓶颈选择大范围破坏区域。
- 程序生成官方最小方案接口，不改算子语义、不增加硬件广播、不实现问题二/三的缓存机制。

下一步应在冻结预算后运行全量对照实验，优先检查分组粒度、缓存压力代理和共享输入导致的边界流量，再决定正式论文中的方法与参数。
