\section{问题三：共享 FIFO 缓存下的多处理器调度}
\subsection{缓存状态与双带宽事件模型}
问题三继承场景 B 的合法方案集 $\mathcal P_B(k)$、跨核释放与核内内存约束，新增容量为 $C$ 的共享只读 L2。它不是每核各有一份，也不改变已生成的逻辑 COPY 数量。对时刻 $t$ 按进入先后排序的逻辑张量队列记为 $Q(t)$，有
\begin{equation}\label{eq:q3-capacity}
\sum_{x\in Q(t)}s_x\le C,\qquad C=1\,048\,576\ {\rm bytes}.
\end{equation}
正字节 \texttt{COPY\_IN} 事件 $e$ 在发射时刻 $a_e$ 查询逻辑张量标识。若标识已在 $Q(a_e^-)$ 中则 $h_e=1$，改由 L2 服务；否则 $h_e=0$，仍由 DDR 服务。命中不刷新 FIFO 次序。搬运在完成时按官方处理顺序进行填充：未驻留且大小不超过 $C$ 的张量，从队首淘汰至有足够空间后入队；超容量张量旁路。两个同时发射的冷读不能因为较早一个尚在途中而合并为一次命中。完成时的重新插入、相同时间事件次序等均保持官方语义。

设 $A_D(t),A_L(t)$ 分别为 DDR 和 L2 池的在途操作集合，$b_D=60$、$b_L=250$ bytes/cycle。每条搬运按所属池的独占带宽形成初始服务工作量，活跃集合不变的时间段内，各自池内公平分享服务：
\begin{equation}\label{eq:q3-service}
\dot w_e(t)=-\frac{1}{|A_r(t)|},\qquad
e\in A_r(t),\quad r\in\{D,L\}.
\end{equation}
事件到达或完成后须结算剩余工作并重算完成时刻；实际评价还依官方整数周期取整。两池独立，不能把 $60+250$ 当作共同带宽，也不能给每个核心各分配一条 $250$ bytes/cycle 的 L2 通道。L2 命中只改变 COPY 的服务池，不能绕过 \eqref{eq:q2-release} 中的跨核同步，不能占用别的 Pipe 插队，亦不能使超出 L1/UB 的物理实例合法。

于是问题三在相同合法方案集上求
\begin{equation}\label{eq:q3-objective}
\min_{p\in\mathcal P_B(k)}
\operatorname{lex}\bigl(T_i^{L2}(p;C,b_L),D_i(p)\bigr).
\end{equation}
字节命中率为
\begin{equation}\label{eq:q3-hit}
H_i=\frac{\sum_{e:\ {\rm COPY\_IN},\ s_e>0}h_es_e}
{\sum_{e:\ {\rm COPY\_IN},\ s_e>0}s_e}.
\end{equation}
分母为零时只在统计程序中定义为零并标注“无有效读取”。官方命中率按字节而非按次数统计。$D_i$ 是插入额外逻辑 COPY 的字节量，不因命中自动扣减；同一方案分别在无 L2 和 L2 下评价，该字段应相同。命中字节可作为实际 DDR 服务减量的辅助诊断，但 $H_i$ 越高未必总周期越短，因为同步、计算、L2 带宽竞争以及关键路径变化仍可能占主导。

\subsection{固定方案消融与缓存事件引导搜索}
首先将问题二的 $500$ 份方案在无 L2 的场景 B 与启用 L2 的场景下分别官方重放。两次使用相同划分、映射和顺序，因此其差异刻画固定方案上的硬件效应。随后以该 Q2 方案为保底，在真实 L2 时间线中定位反复未命中的张量与相关消费子图，有限尝试合法的核心迁移和空隙插入；普通关键度候选保留为后备。新方案再由完整 L2 官方事件评价，并按周期优先、同周期较少逻辑搬运接受。每个配置最多评估四个新增候选；继承 Q2 种子的既有求解成本与本问新增成本分别记账。

这种拆分避免把“硬件新增 L2”与“因 L2 改变调度”混成一个不可解释的总效应。固定方案比较不需要宣称算法等预算公平；新增邻域是否优于另一种搜索方式则必须在同一候选机会下单独验证。三张开发图的同预算普通邻域对照为二胜六平一负，因此本文只报告最终统一规则的百图成绩，不把该小样本写成普遍优势。开发集中将本问候选上限从四增为十二也未使九个配置的采用周期继续下降，这只是局部预算检查，不是搜索收敛证明。

\subsection{百图结果与指标边界}
表\ref{tab:q3-main}把无 L2、固定 Q2 方案启用 L2 和重新调度后的 L2 三列并列。前三列加速比均使用同一个无 L2 整图单核基准；后两列则先在每张图、相同核数上计算时间比，再对百图取算术平均。故“五核两个平均加速比之比”不等于“逐图同核时间比的平均”。五核优化 L2 的平均加速比为 $4.240375$，无 L2 对照为 $4.159974$；其逐图同核时间比均值为 $1.022567$。
\begin{table}[!htbp]\centering
\caption{问题三的百图成对评价（各比值均逐图计算后平均）}\label{tab:q3-main}
\begin{tabular}{rrrrrr}
\toprule
核数 & 无 L2 加速比 & 同方案 L2 & 优化 L2 & 同核硬件比 & 同核综合比\\
\midrule
1 & 1.000000 & 1.008622 & 1.008622 & 1.008622 & 1.008622\\
2 & 1.981113 & 1.984989 & 1.986721 & 1.001750 & 1.002467\\
3 & 2.815714 & 2.828518 & 2.834213 & 1.004810 & 1.006671\\
4 & 3.550822 & 3.586810 & 3.598194 & 1.011699 & 1.014923\\
5 & 4.159974 & 4.227368 & 4.240375 & 1.019090 & 1.022567\\
\bottomrule
\end{tabular}
\end{table}
\begin{figure}[!htbp]\centering
\includegraphics[width=0.72\linewidth,height=0.70\textheight,keepaspectratio]{../图表/runs/20260924-A-q3-delivery-r03/speedup.pdf}
\caption{无 L2、固定方案 L2 与优化方案 L2 的平均加速比}\label{fig:q3-speed}
\end{figure}

五核百图总周期在无 L2 时为 $65\,769\,876$，固定方案启用 L2 后为 $64\,721\,316$，进一步调整方案后为 $64\,562\,282$。全 $500$ 个配置中，新增搜索使 $68$ 组周期降低，其他组保留不劣的已评价方案；它不保证相对于无 L2 都改善。固定方案启用 L2 有六组退步，最终优化方案仍有四组比无 L2 慢，不能隐藏。全五百组逻辑额外搬运量从无 L2 方案的 $5\,630\,069\,992$ 增至优化 L2 方案的 $5\,634\,488\,280$ bytes，增加 $4\,418\,288$ bytes；这种次指标代价与周期优先的目标一致，但应在方案比较时明确呈现。图\ref{fig:q3-gain} 给出按同核配对的收益，而不是从总体命中率直接推导周期百分比。
\begin{figure}[!htbp]\centering
\includegraphics[width=0.72\linewidth,height=0.70\textheight,keepaspectratio]{../图表/runs/20260924-A-q3-delivery-r03/cache_gain.pdf}
\caption{相同核数下的 L2 硬件效应与调度后综合时间比}\label{fig:q3-gain}
\end{figure}

\subsection{反例、灵敏度与检验}
第 $44$ 图单核从无 L2 的 $154\,407$ 降至 $107\,814$ cycles，$216$ 次命中、$4\,408\,416$ 命中字节全部来自溢出后的重新读取，说明共享 L2 的作用不局限于原始输入复用。以同一方案改变容量时，结果见表\ref{tab:q3-capacity-scan}。这是事后机制诊断，揭示阈值与局部非单调，不是独立确认实验。FIFO 对容量不具备一般的命中集合嵌套性质，相关经典讨论可参见 Belady 等\cite{belady}；本题具体数值只来自自己的官方事件重放。
\begin{table}[!htbp]\centering\small
\caption{第44图单核固定方案的 L2 容量检查}\label{tab:q3-capacity-scan}
\begin{tabular}{rrrrrrrr}
\toprule
容量（KiB） & 0 & 64 & 128 & 256 & 512 & 1024 & 2048\\
周期 & 154407 & 154272 & 154407 & 154407 & 134726 & 107814 & 107814\\
\bottomrule
\end{tabular}
\end{table}

第 $5$ 图五核固定方案在 L2 带宽分别为 $60$、$125$、$250$、$500$ bytes/cycle 时，周期分别为 $48\,200$、$48\,202$、$48\,259$、$48\,253$。带宽变化会改变发射与完成次序，进而改变冷读重叠、FIFO 状态和关键路径；不能从“服务速度变快”直接断言固定计划 Makespan 必单调。主实验始终使用题目固定的 $1$ MiB、$250$ bytes/cycle。最明显的固定配置退步为第 $67$ 图双核：无 L2 $32\,040\,130$，有 L2 $32\,217\,720$ cycles。时间线复核将差异定位于搬运关键路径，但该观测分解并非独立因果效应估计。

全部 $500$ 组均完成无 L2/L2 官方成对评价、胜出方案再次回放、FIFO 事件账本和全局物理内存检查；无 L2 周期与问题二冻结指标逐项相同。官方测试集上的回归一致性强于单个演示图，却仍不是未见图的泛化保证。本模型解释固定参数下的结果，未求得所有合法划分上的全局最优，也未把仿真周期当作真实芯片实测时间。
