> **数据已同步，尚待提交验收**：正文、曲线及附录使用20260926完整交付矩阵；编译和视觉检查状态以该目录的记录为准。冷启动仅完成压力样本，未证明100图全核数均在十分钟内完成。旧DOCX未同步，不能用其替代本次PDF。

# 论文写作与整合

当前选择 XeLaTeX/PDF：正式入口为 `论文正文.tex`，最终输出为 `数模论文.pdf`。模板演示与 Word 准备材料在仓库 `output/`，不能当作正式参赛论文。

- B 按 A 派稿指定的 `章节/` 文件逐问写作；不要自行建立另一份全篇入口或重复模型结果。
- C 只读章节与实际编译页面，意见放 `审查/章节格式/QN/`，B 修订后 C 复查。
- A 审核所有章节，统一符号、公共文献、图表接口和全文入口，最后写摘要、整合编译。
- 正式章节名、题数和路径由 A 根据题面确定；当前不预造章节结果。图表从有效证据引用，不手填示例数值。

按 [正赛协作协议](../协作/正赛Agent协作协议.md) 和 [逐问写作核对](../协作/写作核对/README.md) 交付。复制模板到正式论文后在本目录编辑，不把手工正文写入会重建的 `模板/`。
