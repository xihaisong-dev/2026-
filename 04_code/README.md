# 可复现程序

src 放公共模块，configs 放实验配置，scripts 放处理/训练/求解/绘图入口，tests 放需要的验证，notebooks 仅用于探索。data/processed 是可重建的本地数据，不提交默认大文件。

技术栈确定后在本目录添加真实依赖文件和完整运行命令；暂不虚构依赖。脚本从明确工作目录运行，用相对路径配置数据位置。随机实验记录种子，最终输出写到新的 05_results/runs/<run-id>。
